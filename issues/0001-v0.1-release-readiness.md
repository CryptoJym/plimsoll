# 0001 — Parent: v0.1 open release readiness

## TL;DR
- Plimsoll's public debut: macOS collector + Claude Code/Codex capture + local efficiency reports, with CI enforcing the signal-fidelity proof.
- Child lanes: 0002 (CI), 0003 (enrollment polish), 0004 (codex live verification), 0011 (npm packaging), 0013 (privacy spec page).
- Closes when a stranger on a clean Mac can go from `curl install.sh` to a real `pnpm report` number without touching a maintainer.

## Scope
v0.1 = single-machine, macOS, Claude Code + Codex, local reports. Hosted sync exists but is dormant without an `uploadUrl`. No Linux, no extra agents, no benchmark features.

## Context
- Capture path proof-gated: `pnpm proof` (14 checks, evidence/ artifacts).
- Real-world verification: $48.46 / 230k tokens attributed to a real PR via branch-hash linkage on 2026-06-10.
- Collector runs as LaunchAgent `com.plimsoll.collector`, ledger at `~/Library/Application Support/Plimsoll`.

## Acceptance Criteria
- [x] All child issues closed or explicitly deferred with a note here. *(current ledger below, 2026-09-01)*
- [ ] Fresh-Mac walkthrough recorded as a transcript in evidence/ (install → session → status shows token coverage → report). **BLOCKED in this lane** — see W-2; no clean-Mac or live-Codex evidence is claimed.
- [x] README quickstart matches the in-tree executable command contract. *(source/manifest proof: `pnpm proof:release-readiness`; live registry and clean-Mac execution remain UNKNOWN.)*

## Operational Boundaries
- `pnpm proof` green on every commit to main.
- No raw content persists in metadata mode — any new field needs a forbidden-list entry + sentinel check.

## Notes For Future Agents
The capture path's history: v1 silently captured ~0% of tokens for five weeks because nothing asserted signal quality on real sessions. That's why the proof is the release gate, not unit tests.

## Child-lane readiness ledger (lane 0001 audit, 2026-09-01)

Statuses below are bounded to this checkout. Hosted, registry, clean-Mac, and live-provider
claims are marked UNKNOWN or NOT_RUN rather than inferred from source.

| Child | Status | Evidence / reason |
|---|---|---|
| 0002 CI | implemented in-tree; hosted-green observation UNKNOWN | `.github/workflows/proof.yml` triggers on `pull_request` and `push` to `main`, uses `macos-14`, installs from the frozen lockfile, runs `pnpm proof` plus the focused gates, and uploads `evidence/` with `if: always()`. Hosted completion time and a live mutated-PR failure are NOT_RUN here. |
| 0003 enrollment polish | implemented under the superseding `setup` command | `packages/collector-cli/src/cli.ts` provides dry-run diff planning, confirmation/`--yes`, backups, and `setup_noop`; `install.sh` chains `pnpm collector setup --yes`; doctor compares generated Claude/Codex config and connectivity. The literal historical `apply-config` command and `install.sh --apply` flag do not exist and remain intentionally deferred as stale names. |
| 0004 Codex live span verification | OPEN — deferred to v0.2 | A real Codex session, `tokenCoverageLast7d` observation, and live span shape are NOT_RUN in this lane. Source fixtures and trace handling are not a substitute for that evidence. |
| 0011 npm packaging | partially implemented — publication workflow and clean-Mac smoke deferred | `packages/collector-cli/package.json` contains the scoped package name, `plimsoll` bin, `dist` files, Node engine range, and public publish config; README is npx-first. No tag-triggered provenance workflow is present, and global install/`plimsoll install` is NOT_RUN. Registry state is NOT queried in this lane. |
| 0013 privacy spec page | implemented in-tree | `docs/privacy-spec.md`, `scripts/privacy-spec.ts`, `scripts/privacy-spec-proof.ts`, `docs:privacy`, and workflow freshness checks are present. |

### Walkthrough blocker note (W-2)

The required transcript needs a real clean macOS machine and a real token-bearing
Claude Code or Codex session. This bounded lane has neither a clean-Mac surface nor
authority to run `install.sh` against the operator's home, LaunchAgents, or tool
configs. The source-only command proof and existing isolated proofs establish static
and synthetic contracts only; they do not close the Fresh-Mac acceptance criterion.
