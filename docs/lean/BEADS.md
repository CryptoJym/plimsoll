# Implementation decomposition (round 11 = B0 round 5 corrections on round 7)

**Bead:** eco-6hoxj.164.2 (program plan). Children below are proposed under `eco-6hoxj.164`. Owner type: **collector** (plimsoll repo, `packages/collector-cli`), **cloud** (plimsoll-cloud), **both**, **external** (a running lane this program depends on). Effort is worker-hours for one Sol- or Opus-class worker at default effort unless noted; elapsed time is separate where an observation window, a rollback window, a measurement window or a rehearsal dominates. Model routing follows the shared policy (Sol/Opus for implementation, Astra/Fable for planning and integration). Every bead returns a bundle with `REPORT.md`, checks, `MANIFEST.sha256` and `RESULT.json`, and a Jev work registration.

**What changed in round 5.** B0's failing tests are seeded from the eight runnable fixtures in `out/fixtures/` (one per blocker and two for should-fixes; `out/checks/fixtures-summary.json`), which B0 ports into the repository that owns each rule. B2a gains the identity-row-plus-edges membership, the `autoincrement` target key with `PRAGMA foreign_keys = ON`, the retirement proof, the epoch-keyed day tables and the binding-version stamp (B1-B4). B6 gains `binding_version`, the response field, the round-5 part bases and the open gap; B8's export reads the bases and the unallocated section (B4, should-fix 5). B9a runs the shape census and the per-cutoff harness (B5). B15 adds the token volume state and the unknown-volume gates (B6). **Effort and calendar are uncalibrated** until the copy and membership-exchange drills run (should-fix 4; §1 totals, §2). B18's fleet owner is recorded with its acceptance state (should-fix 3). The first wave is the reviewer's list, unchanged.

**What changed in round 6.** The reviewer's five `b0Carries` items are B0's first work and each has a fixture: the day-target item and receipt (`out/fixtures/b1_day_target_receipt.py`), actor ownership across an offline rebind (`b4_offline_rebind.py`), every schema-valid timestamp in the day facts (`b5_non_iso_day_facts.py`), G and the hold-growth multiplier from the round-5 row geometry (`s1b_runway_geometry.py`), and B22 plus PROOF §8 aligned with the open gap (`b22_false_complete.py`); two should-fixes have fixtures too (`sf_no_cache_columns_guard.py`, `sf_abort_rebuild_bound.py`). B6 gains the `day_summary` item, ingest-by-stamp and `heard_at`; B2a the stamp on the wire and the non-ISO fold; B21 targets collector 0.7.42; B22 prescribes `epoch_open`. **The start order is the reviewer's round-5 first wave** and its freezable list is recorded in §3; B18's fleet half stays gated until an accepted native owner receipt exists. Effort and calendar remain uncalibrated.

**What changed in round 7 (B0, eco-6hoxj.164.4).** B0 repaired the two round-6 blockers and the four `b0Carries` items as contracts (`CONTRACTS.md` C1-C5), corrected `b4_offline_rebind.py` (one predicate, `heard_at` = first echo; round 8 corrects the echoing request to the 400 delivery) and added `s1b_runway_host_bound.py` (per-host G from counted cardinalities, one G, G remaining), ported every fixture as a **pending** test into the repository that owns its rule (branch `lane/eco-6hoxj.164.4` on plimsoll `03445d3a` and plimsoll-cloud `4954995`; `CONTRACTS.md` C6 lists the surfaces each test binds), and wrote the freeze list (`FREEZE.md`). B1 gains the one-time cardinality census on a ledger copy; B2a the `conversion_rejects` DDL, the null stamp before the first response and the stamp diagnostics; B6 the single actor predicate and the `heard_at` = first-echo rule.

**What changed in round 8 (B0 round 2, eco-6hoxj.164.4).** (Round 9 and round 10 notes: `CONTRACTS.md` header, `CHANGES.md`.) The round-1 review of B0 required three repairs before signature: the actor predicate still depended on when a row was judged (`CONTRACTS.md` C1/C4: the stamp is now the pair `(install, version)`, its answer is fixed at the first sighting and recorded as `stamp_not_issued` when the version was not yet issued, and the collector clears the pair at a join, re-join or transition), `G_remaining` counted dual-write pages as conversion and ignored rows admitted after the census (`CONTRACTS.md` C2: `G_owed` from the converter's own bytes plus the unfolded post-census rows; the census after the catch-up, fresh, space-gated, with a segment proxy), and the test helper refused every fixture append (`tests/contracts/lean/_pending.ts` pins the enrollment epoch; `helper.contract.ts` guards it). Also: `conversion_rejects` in the never-delete set with its `raw_rowid` nulled by the trigger (C3), the `<UR>` pin test in both repositories (C8), the fixture's r6 mode on round 6 as written, `heard_at` from every request (the 400 delivery), and the hygiene items. Beads: B1 builds `G_owed` and `censusPreflight` (not round 7's rule 3); B2a the pair, its scope, `lean/usage-record.ts` and the trigger; B6 the sighting fact, judging by the pair and `heard_at` from deliveries; B10b refuses to release a raw row with an open reject.

## 1. Beads

| Id | Result | Owner | Inputs | Depends on | Acceptance | Effort | Start now? |
|---|---|---|---|---|---|---|---|
| **B0** Contract repair and ownership | The `<UR>` fixture pinned from the inline predicate at cloud `067a8a4` (`loader.ts:72-79`) with a test that re-pins to `USAGE_RECORD_PREDICATE_SQL` when lane 2 merges (`LANES.md:62`); protocol v2 items with segments (the `day_summary` item and receipt included), per-actor parts with the stamp on the wire and ingest-by-stamp, segment assignment, lacking-id lists, the coverage relation and the residue proof; claim v2 with tailer and fault fields; local DDL v3 (epoch-scoped keys, segments, tombstones, tagged model key rejecting `m:`, `token_rule`, `observed_day`); dashboard contract v3 with the UTC day key; the rate-catalog contract (price, ceiling, alias); versioned reader semantics; the deletion proof and the seal lifecycle; **the round-5 fixtures ported as failing tests** into the repository that owns each rule (`out/fixtures/b1_membership_edges.py`, `b2_epoch_day_keys.py`, `b3_durable_target_refs.py`, `b4_actor_at_capture.py`, `b5_lexical_boundary.py`, `b6_unknown_volume_gates.py`, `sf1_ladder_rollback_parity.py`, `sf5_unresolved_file_open_gap.py`; each red under `--rule r4`, green under `--rule r5`) **and the seven round-6 fixtures** (`b1_day_target_receipt.py`, `b4_offline_rebind.py`, `b5_non_iso_day_facts.py`, `s1b_runway_geometry.py`, `b22_false_complete.py`, `sf_no_cache_columns_guard.py`, `sf_abort_rebuild_bound.py`; each red under `--rule r5`, green under `--rule r6`; `out/checks/fixtures-summary.json`) plus `out/checks/counterexamples.py` cases 1-19; the ownership map of every shared surface. **Round 7 (done in eco-6hoxj.164.4):** the contracts C1-C5 in `CONTRACTS.md`; `b4_offline_rebind.py` corrected and `s1b_runway_host_bound.py` added (red r6, green r7); every fixture ported as a pending `node:test` in its owning repository (`CONTRACTS.md` C6; `checks/collector-contracts.log`, `checks/cloud-contracts.log`), each naming its bead; the two reviewer counterexamples as tests; `FREEZE.md`. **Round 8 (B0 round 2, done in eco-6hoxj.164.4):** C1/C4 repaired (the pair, the first-sighting rule, the re-join scope), C2 rule 3 repaired (`G_owed`, the census obligation, the segment proxy), C3 completed (never-delete set, `raw_rowid`), C8 (the `<UR>` pin in both repositories), the test helper fixed with a guard, the two fixtures rewritten (red r6 and r7, green r8), the review's should-fixes, and `FREEZE.md` updated. **Round 9 (B0 round 3, done in eco-6hoxj.164.4):** the review-r2 blockers closed (C4: the joined install, the old install's late answer ignored; C1: the sighting serialized against the rebind, reproduced in Postgres; the two unpassable pending tests repaired with helper guards) and its nine should-fixes (the fact keyed by the uploader, retention and erasure of facts, audit rows and installs, the pending Postgres proof, the honest-collector wording, `C_conv` measured, the during-hold copy gate, the pin's case folding, the schema test's columns, the documents); the two fixtures extended (red r6, r7 and r8, green r9); `FREEZE.md` updated. | both (planning lead) | ARCHITECTURE.md §2.2, §3, §5, §6, §8, §10; `out/fixtures/` | none for the documents; the predicate re-pin waits for lane 2 | Lead sign-off; fixture hash identical in both repos; a failing contract test per item kind, per reader and per fixture; lane 2's status (merged or not) recorded with the pinned text. | 6-8 h (uncalibrated) | **Yes**; documents and tests only (touches no product path; the fixture test lives beside the inline predicate). No contract is frozen by B0 alone: sign-off is the lead's after the tests exist, and per the round-5 review the protocol, actor, timestamp, deletion and runway contracts are not signed off until the round-6 fixtures are ported and green in both repositories. |
| **B1** Telemetry, observe-only (running as eco-6hoxj.164.3; round 7: plus the one-time cardinality census on a light host's ledger copy, `fixtures/host_cardinality_census.py`, as the basis of `G_host`; round 8: the census after the catch-up, at most a day before S1b, again before S3, space-gated, with the segment proxy; the converter's written-bytes counter and the unfolded post-census raw bytes for `G_owed`, never the lean tables' page count; `censusPreflight`; `runway.g` in `/status`; round 9: the re-census copy during the hold gated on `free − ledger ≥ reserve + rebuild headroom + G_owed` (`copy_would_trip_rung`), `C_conv` read from the converter's measured checkpoint, `U` in the census's bytes per raw row; `CONTRACTS.md` C2; pending test `tests/contracts/lean/runway.contract.ts`) | Samples ledger bytes, `page_count`/`freelist_count`, free volume bytes, RSS per process, CPU deltas, WAL bytes/min and 24 h high-water, outbox ages, summary lag, per-source `bytesDeferred`/`deferredGenerations`, coverage-walk age and completeness, unread-file classes, open faults, every 60 s into a ring table and `/status` (`budget.mode = advisory`); daily gross ledger growth, the activity-to-usage ratio, and once seven days exist `hold_growth_per_day` and `runway_days` (BUDGETS.md §4.2-4.3); daily `dbstat` on light hosts only. | collector | BUDGETS.md §4.3; ARCHITECTURE.md §2.4, §6.4; `collector-cli/src/server.ts:1088-1135`; `rollout-tailer.ts:1263-1289`; `buffer.ts:3035-3039` | none | 7 days of samples on Studio5 and Studio6 as CSV with p50/p95 and the derived runway; per-process RSS; the WAL high-water; no heavy scan on Studio0; nothing enforced. | 5-6 h (2 h added for the growth and runway derivations) | **Yes**; already running; the runway derivation is an additive change to the same module. |
| **B10a** Hold: code and copy test only | The `raw_retention_control.hold_reason` early return, `retention.hold` in `/status`, the ladder's acknowledged-only predicate (`collector-cli/src/outbox.ts:662-671,701-709`) and the red/green test on a ledger copy (ARCHITECTURE.md §2.4). **No host configuration**: `retentionDays = 3650` and the hold are set per host at S1b under the runway rule, never earlier. | collector | `collector-cli/src/buffer.ts:2997-3013,3041-3052,3108-3123`; `config.ts:67` | none for the code; B0 for the column name | PROOF.md §5 items 1-2 on the Studio5 copy; `/status` shows cutoff, hold and the ladder rung; the S1b gate evaluable. | 3-4 h | **Yes**; one column, one early return, one predicate and a test; nothing deployed. |
| **B2a** Local schema v3, writer and converter, copy-ready | Tables in ARCHITECTURE.md §3 created; the lean connection opened with `PRAGMA foreign_keys = ON`; writer at admission behind `lean.write`: fact keyed by `(event_id, raw_generation)` with the live pointer, `epoch_key`, `observed_day`, `token_rule`, `rate_bound_kind`; one `summary_members` identity row per raw row with its `row_class` and the `actor_binding_version` stamp (also written into the outbound envelope as `metadata.actorBindingVersion`, with `actorBindingVersionHeard` and, round 10, `actorBindingInstallHeard` on every request, ARCHITECTURE.md §5.1; round 10, `CONTRACTS.md` C1/C4: the join request's `previousInstall` proof, the handshake's version recorded only for the grant's install, `seedJoinedInstall`, `actorBindingStatus` with `join_incomplete`, `partitionFloodRefusal`; round 11: `partitionRefusedRows`, `parkOutboxRows`/`parkedRows`/`releaseParkedRows` for a `stamp_from_other_ledger` refusal, `actorBindingStatus` with `lineage` and `parkedRows`), and exactly two `summary_member_edges` rows (base target + day target); segments keyed by `autoincrement`, the seal rules, tombstones through the raw-delete trigger updating every covering target, the last-chunk edge drop and the zero-reference retirement proof, `sealed_member_mutations`, the re-admission rule on tombstones; the epoch-keyed day tables under one day target per (epoch, source, UTC day); turn and session summaries with `m6_v3`/`m8_v2` fields and `parts_by_local_actor_json`; day-grain facts keyed by the UTC day of the parsed instant for **every** schema-accepted timestamp, non-ISO forms included, with `conversion_rejects` for a string the parser refuses (ARCHITECTURE.md §3.5; round 8: its `raw_rowid` nulled by the raw-delete trigger, the table and open-reject raw rows in the never-delete set, `CONTRACTS.md` C3); the stamp as the pair `(install, version)` scoped to the install, cleared at a join, re-join or transition, with `actor_binding_install` on the identity row and `metadata.actorBindingInstall` on the wire (round 8, `CONTRACTS.md` C1, C4); the **joined install** recorded at join activation (`recordJoinedInstall`, `collector_workspace_binding.joined_install`), a versioned response accepted only from it and any other install's ignored and counted, the daemon reading it from the ledger and never from its start-up config (round 9, `CONTRACTS.md` C4); `withMeasuredWrite` measuring the converter's `C_conv` as its page allocation per chunk (round 9, `CONTRACTS.md` C2); `lean/usage-record.ts` with the pinned `<UR>` predicate (`CONTRACTS.md` C8); rollups with the tagged key; gaps with `interval_basis`; the resumable, throttled converter with the compact-segment fold and re-keying; correlation helper extracted from `runtime-facts.ts`. Proved on **copies** (Studio5, Studio3) through S2 and S3 with the exhaustive certify. | collector | ARCHITECTURE.md §2.2, §3; `collector-cli/src/buffer.ts:2401-2416`; `session-summary.ts:168-215,277-347` pattern; `runtime-facts.ts:161-230`; `dashboard-projection.ts:835-846,1905-1949` | B0, B10a | Unit tests per table and per rule (seal, tombstone, residue, re-admission, edges per row class, retirement proof, m6_v3, m8_v2, d_v1, UTC day, boundary classes, tagged key, epoch key); the round-5 and round-6 fixtures and the reviewer's counterexamples green; a copy of Studio5 and of Studio3 converted and certified (MIGRATION.md S3 certify (a)-(d)) with nothing deleted. | 16-20 h (**uncalibrated**: several tables, triggers, the converter and two copy certifies; re-estimated after the Studio5 copy work) | After B0 and B10a. Shared surfaces: one admission hook in `buffer.ts`; raw-row triggers on `buffered_events` (shared with session sync and dashboard triggers); the correlation helper (0.7.41 learning-facts owner). Prototype on a fixture now. |
| **B2b** Live dual-write acceptance (S2) | The B2a release on a live host behind `lean.write` for 24 h with the runway alarm on; MIGRATION.md S2 (a)-(e). | collector | MIGRATION.md S1b, S2 | B2a, B13, B1 (7 days of samples on the host), B21 (catch-up complete on the host) | S2 gate green on Studio5; growth and runway recorded. | 2-3 h + 24 h elapsed per host | After S1b on the host. |
| **B3** Local history backfill and certify (live) | B2a's converter run on the live host under the throttle (MIGRATION.md S3): every raw row and compact segment converted, historical segments sealed, the day-key correction enumerated against the S0 census, exhaustive certify, peak growth, WAL and runway recorded. | collector | MIGRATION.md S3; BUDGETS.md §4.2 | B2b, B9a, B21 | Studio5 and Studio3 fully converted and certified live; throttle pauses observed with reasons; nothing deleted; the runway above 10 days throughout. | 4-6 h + elapsed per host | After B2b. |
| **B4** Finance publication on `usage_facts_local` | `finance-project-usage-projection.ts` reads the fact table through the same `readFacts` shape and lineage checks behind `lean.read.finance`; retained-live trigger re-sourced; the configured maximum publication period recorded as the fact-retention floor. | collector | ARCHITECTURE.md §2.1, §3.1, §8; `finance-project-usage-projection.ts:488-519,811-895` | B2a, B3 | Same-instant equality of the finance publication on both readers on Studio5 and Studio1 (rows, reason codes, totals); red/green per reason code. | 6-8 h | After B3 (fixture work can start after B2a). |
| **B5** Dashboard snapshots schema 3 | UTC-day-aligned windows from the day tables with the UTC day key; `DASHBOARD_SCHEMA_VERSION` 3; `lean.read.dashboard`; `lean.facts=off` path that stops the fact copy and its per-row enqueue; the census-enumerated parity (PROOF.md §4). | collector | ARCHITECTURE.md §3.5, §8; `dashboard-projection.ts:43,545-551,2047-2076,2185-2196,3635-3806,1170-1242` | B2a, B3, B9a; sequenced after the `.163.84` projection-convergence merge or under its integration owner | MIGRATION.md S5 and S8 gates on Studio5, Studio3, Studio1, Studio4; PROOF.md §4 dashboard rows (a), (b), (c) and the offset fixture row. | 12-16 h | **No**: shared `.163.84` surface. |
| **B6** Cloud contract v2 | `activity_summary_v2` payload kind on the ingest route (zero-item batches allowed), strict schema, tables (summaries, segments, day rows, rollups with `NULLS NOT DISTINCT` and the tagged key, gaps with a nullable end and `resolved_at`, faults, quarantine), `segment_ref` on activity rows with the assignment rules and lacking-id lists, `device_installs.binding_version` incremented with every audit row and the migration that numbers existing audit rows, `device_installs.ledger_install_id` set by the join route from a verified `previousInstall` proof (round 10, `CONTRACTS.md` C1 "Scope"), the erasure-only delete trigger on `device_installs`, `lockInstalls` ordered, `loadSightingView` (lock first, then read) and the ledger-keyed `stamp_not_issued` fact listed before `DeviceInstall` in `TENANT_COLUMN_MODELS` (round 10), `actorBindingVersion` on every authenticated response (`cloud src/lib/delivery-ack-response.ts:27-41`), `ai_summary_actor_parts` with the round-5 bases (`raw_ingest`, `binding_at_capture`, `single_binding`, `unallocated_no_stamp`, `unallocated_stamp_invalid`, `binding_history`) and the stamp verification, **ingest-by-stamp** for delivered events (`metadata.actorBindingVersion`; `actor_id = null` plus `actorStampInvalid` for an invalid stamp), `actorBindingVersionHeard` recorded as `heard_at` on the audit row from every request kind, deliveries included (round 8), the **`stamp_not_issued` fact** recorded at the first sighting of a pair above the install's version and read by `actorForStamp` for ever after, ingest and parts judged by the `(install, version)` pair against the install it names (round 8, `CONTRACTS.md` C1), the sighting reading the install row `FOR SHARE` so the rebind cannot slip between the read and the fact, the fact keyed by the **ledger** (`ledger_install_id`, `CONTRACTS.md` C1 "Scope"; round 11 corrects this row's stale "uploader", read c1 r10 should-fix 1) with `tenant_id` and a foreign key to the install, a pair naming an install outside the uploader's ledger refused before any judgment with nothing recorded (400 `stamp_from_other_ledger`; the sighting reads the named install's ledger under its lock), `linkLedgerByAdmin` as a whole-ledger merge into a root of the same tenant (round 12, `CONTRACTS.md` C1 "The link is the release": every install of the source ledger moved in one transaction under one set of locks, `ledger_linked_at`/`ledger_linked_by` on every moved install, the source ledger's facts re-keyed, one audit row in `device_install_ledger_links` listed in `TENANT_COLUMN_MODELS`; a member, a non-root destination and another tenant's install refused), `linkLedgerAtJoin` holding the previous install's row `FOR SHARE` (round 12), `lineage` on the join grant and `lineage` with `ledgerInstallId` on every acknowledged response (round 12), the join route's `previousInstall` bound to its token (round 11, `CONTRACTS.md` C1, C6), at most 8 new facts per request, facts, audit rows and installs kept until tenant erasure with the fact table in the erasure list and the residue check, proved on the disposable Postgres cluster (`actor-binding-stamp-postgres.contract.test.ts`; round 9, `CONTRACTS.md` C1), the **`day_summary` item and receipt** (ARCHITECTURE.md §5.2), per-item receipts with segment status and the coverage field, monotone revision, seal-once and digest rules, the paged id exchange, claim v2 with `declaredGaps`, `faults` and tailer fields, coverage false while a fault or an open gap exists, tenant erasure additions, `activity_lane_closed`. | cloud | ARCHITECTURE.md §5-7; `cloud src/app/api/work-intelligence/ingest/route.ts:48-111`; `ingest.ts:397-447`; `capture-watermark/store.ts:24-58`; `contract.ts:68-105,407-424`; `subject-rights.ts:470-602`; `device-install-actor-binding.ts:165-198,270-318` | B0; coordination with .163.67 (watermark) | Contract test: a batch with every item kind judged with receipts; a sealed segment never replaced; a conflict receipt leaves the watermark unchanged; a persisted gap, an open gap and an open fault keep coverage false through a later claim (`out/fixtures/sf5_unresolved_file_open_gap.py`); the B1 residual fixture (`out/fixtures/b1_day_target_receipt.py`) green: the day target acknowledged only through its receipt, a conflict quarantined; the B4 fixtures (`out/fixtures/b4_actor_at_capture.py`, `b4_offline_rebind.py`) green: undelivered members by stamp, pre-stamp members unallocated or single-binding, no `summary_ingest` basis, a binding change after ingest moves no part; parts sum to the segment digest; erasure leaves zero residue in the new tables; the lane-closed refusal. | 16-20 h (uncalibrated) | After B0. Shared surfaces: `ingest/route.ts` dispatch (.163.67 owner), `schema.prisma` (after lane-2 models), `subject-rights.ts` (after lane 2's addition), `capture-watermark/`. |
| **B7** Cloud history backfill, certify, assignment and relation | Cloud-origin summaries, segments (boundary = cloud `created_at`), day rows, rollups and parts from existing cloud activity rows, lane-2 house style; `segment_ref` assignment and lacking-id lists for collector-origin segments; exhaustive certify; the stored relation per segment with the `partial` report. | cloud | MIGRATION.md §6; ARCHITECTURE.md §5.2-5.3; `DESIGN.md §4.7` | B6 | Certify passes on a disposable cluster with a production-shaped fixture and on the data tenant; every session and bucket has a relation over all its segments; every activity row has at most one `segment_ref`; export parity for 5 actors plus the A→B→C, A→B→A and undelivered-member fixtures; the unallocated count per install reported; no deletion. | 12-16 h + elapsed (**uncalibrated**: the 1.88 M-row session may need about 380 id-exchange pages; re-estimated after the membership-exchange drill on the fixture) | After B6. |
| **B8** Cloud readers v2 and rights | Admin strip, manager view, experiment snapshot (`metric_version = 2`), `usage_v2` projection with certify and `usage_v1` invalidation, export and impact counts by actor parts (exact bases only) with the unallocated section and the removed-field list, tenant erasure residue; every reader honours the relation (`partial` flagged, never summed) and shows post-seal mutations. | cloud | ARCHITECTURE.md §5.3, §7-8; `cloud src/lib/dashboards/loaders.ts:60-113,270-320`; `mutations.ts:26-40`; `usage_v1` migration `:53-59,139-175`; `subject-rights.ts:74-242` | B6, B7 | PROOF.md §4 rows for each reader, the relation row and the actor-parts row; each behind its own flag. | 16-22 h; splittable by reader | After B7. Disjoint from economics-lane files. |
| **B9a** Oracle, shape census and harness skeleton | The raw-row and compact-segment **oracle** for the five windows (`oracle.sql`) with the day-aligned cutoff, the UTC day key computed from the parsed instant and epoch-keyed groups, proved equal to the schema-2 snapshot at a UTC-midnight instant on Studio5 and Studio1 on their rows outside the census classes, with the boundary rule of ARCHITECTURE.md §3.5 evaluated per non-canonical row and per cutoff, `non_iso` rows folded by their UTC day (`out/fixtures/b5_non_iso_day_facts.py`), and the census described as conservative over a finite corpus; the read-only **shape census** run on every host at S0 (the query of `out/checks/ledger-shape-census.log` plus the class predicates of `out/fixtures/b5_lexical_boundary.py`); the same fixture run under the collector's bundled runtime with any `Date.parse` difference from `out/checks/node-date-parse.log` recorded; the harness skeleton with the fixed query list of PROOF.md §4; the Studio0 baseline rule. | both | MIGRATION.md §3; PROOF.md §4; `out/checks/ledger-shape-census.log`; `out/fixtures/b5_lexical_boundary.py` | none | Oracle equals schema 2 at a UTC-midnight instant on Studio5 and Studio1 (`dashboard-projection.ts:3665-3669` old path); the census recorded per host by class; the query list reviewed by the lead. | 6-7 h (uncalibrated) | **Yes**; reads existing tables only (the census is the same read-only query already run on Studio3); nothing written to the ledger. |
| **B9b** Parity harness, full run, and migration runbook | The harness runs every PROOF.md §4 row on both paths at a frozen instant (UTC midnight for the dashboard) and diffs, including the exhaustive certifies, the identity audits and the relation rows; the per-stage runbook with flags and gates including S1b, the runway and F. | both | MIGRATION.md §2, §5; PROOF.md §4; B9a | B9a, B4, B5, B8 | Harness green on Studio5 for every row; seven daily runs recorded for S6; runbook reviewed by the lead. | 6-8 h (estimate; the exhaustive readers over seven daily runs may take longer) | After B4/B5/B8. |
| **B10b** Deletion proof, tombstones and lean retention job | The retention job implementing ARCHITECTURE.md §2.2 (round 8: a raw row with an open `conversion_rejects` row is never released by the prune or the ladder, `CONTRACTS.md` C3) (every covering target's seal check through the edges, per-row check, identity audits at the boundaries, residue, tombstones, the last-chunk edge drop, the retirement proof, receipts with the new reasons) and the never-delete set, for every target kind; the receipt-table CHECK rebuild in one transaction; today's `prune` predicate replaced; the job is the only code that clears the hold; `/status` lists refused rows, failed audits and post-seal mutations with reasons. | collector | ARCHITECTURE.md §2.2-2.4; `collector-cli/src/buffer.ts:331-338,2997-3105` | B2b, B3, B6 (receipts), B10a | PROOF.md §5 items 3-11 on the Studio5 copy, first and second chunk included, the refused-open-day-target row and the retirement refusals. | 12-14 h (**uncalibrated**: the all-target proof; re-estimated after the Studio5 copy drill) | After B6. Shared surface: `buffer.ts` prune and the receipt table. |
| **B11** Stop activity upload and the fact copy; retention change | `lean.upload_activity` per host (gated on B21's and B22's fields and the relation); `lean.facts=off` (from B5); the F window; usage-fact retention setting (30 or 90 days); metric compaction. | collector | MIGRATION.md S7-S9 | B5, B8, B9b (seven green days), B10b, B21, B22 | S7-S9 gates on the first host; replay of retained rows after a flag-off drill during F; the lane-closed refusal observed after the tenant's first cloud deletion. | 3-4 h | After the S6 window. |
| **B12** Budget enforcement (post-migration), conversion throttle, reserve | Advisory/enforced modes with calibration receipts; conversion throttle with the reserve and runway floors; reduction order; the physically backed usage reserve band and `reserve.present`; storage-full behaviour; 100% backpressure with counted and unknown gaps; RSS worker restart. | collector | BUDGETS.md §4 | B1, B2a, B6 (gap upload), B22 (fault path) | PROOF.md §7 drill on the rehearsal host after S10 including the full-volume item; the throttle test on a pre-migration copy. | 8-10 h | After B2a, B6 and B22. |
| **B13** Rebuild tool with full writer pause, abort recovery and the ladder drill | `plimsoll maintenance rebuild`: preconditions, writer leases and quiesce, 503 + spool for pushes, checkpoint, `VACUUM INTO`, verify, swap, rename-back path, forward-repair refusal after resume; the abort path after a copy-ready S2/S3; the runway ladder drill with the rollback-parity check (PROOF.md §5 item 2b); incremental-copy alternative. | collector | MIGRATION.md §4; `out/checks/writer-modules.log` (31 modules, regenerated this round) | none for the tool; B2a for the abort and ladder drills | PROOF.md §6 drills on Studio5's ledger copy including items 7 and 8; pause time recorded; refusal paths tested; **accepted before any host enters S2**. | 12-14 h (**uncalibrated**: the lease drill over all 31 modules; re-estimated after the first copy drill) | Tool **yes**; standalone script plus the lease protocol design; the drills wait for B2a, not for a live S2. |
| **B14** Receipt, capture-root and tombstone compaction | Batch receipts for acknowledged items whose raw rows are gone; capture-root receipt and tombstone compaction only with a proved dedupe horizon, else explicitly deferred with the proof gap named. **Studio0 gate (round 5):** if the rehearsal's measured tombstone rate, `(1 + activity ratio) × usage rows per day × 125 B`, exceeds 10 MB/day (BUDGETS.md §3), the dedupe-horizon proof and this compaction are on Studio0's critical path before its live window, because indefinite tombstones would otherwise pass the 16 GB target inside a year. | collector | `VALUE-MAP.md:29-30`; `collector-cli/src/outbox.ts:701-709`; `capture-root-inventory.ts:196-258`; ARCHITECTURE.md §2.2 "Tombstones" | B6, B10b | Replay of a dead receipt after compaction still supersedes correctly; a replayed source file mints no fresh consumption; a re-admitted id after tombstone compaction is still refused or quarantined. | 4-5 h | Deferred: not on the critical path; the Studio0 sizing lever if the rehearsal lands above 12 GB. |
| **B15** Gates, dimensional daily rollup and rate catalog | G1-G3 fields (additive) on the economics contract, including the tailer, fault and interval-basis fields, the bounded/unbounded remainders and the three **unknown-volume counts**; `token_rule` and `token_volume_state` on `usage_facts` and normalised tokens in the daily `(project, model_key, account, source)` rollup as `fact_version` 2, with the volume gate evaluated before any share (`out/fixtures/b6_unknown_volume_gates.py`); the `rate_catalog` with **price** entries seeded from `collector-cli/packages/shared/src/pricing.ts:22-52` (fingerprinted, `ceiling_complete = 0`), **ceiling** and **alias** entries attested by an admin per exact model, tier set and interval; the coverage sentences. | cloud | DECISION-METRICS.md §0-1, M2; ARCHITECTURE.md §3.1, §8; `DESIGN.md §4.5, §7` item 6; `maintenance.ts:276-288` | economics lane 3 merged | Parity of the daily rollup with facts sums on the lane-2 fixture; token shares equal a hand computation under each `token_rule`; honesty rendering tests for each gate sentence; a model with a prefix price and no ceiling withholds the dollar comparison; a model with an attested ceiling bounds it; an ended interval does not. | 8-10 h | Spec **yes** now (contract fields, the token rules and the catalog schema with its three entry kinds); code after lane 3 (touches `contracts.ts` and the panel, which lane 3 owns until it returns). |
| **B16** Metrics M3-M9 readers | Budget record and M3 (7-13 day rule); M4 distinct items with supersede and reopen; M5 with the read-time acceptance join, segments summed and the relation; M6 by class and by (class, model) from the capture-time aggregates; M8 by model; M7 with per-source token rules and the catalog; M9 with the dollar, token and zero-baseline rules; page wiring under either roles answer; the allowance "not connected" state. | cloud | DECISION-METRICS.md §2, §4 | B8, B15 | Each metric's gate sentence and number verified on a fixture with known answers, including the M6 overlapping-class and two-model case, the M8 mixed-model case and the M9 zero-baseline cases; UX placement per §4. | 12-16 h | After B15 and B8. Splittable: M3/M7/M9 (rollup only) from M4/M5/M6/M8 (summaries). |
| **B17** Subject rights, local | Local export and erasure cover every new table including segments, tombstones and faults; tombstone counts; the cloud side is in B8. | collector | ARCHITECTURE.md §7 | B2a | Local export contains every session and usage record of the old export; erasure leaves no row in any new table. | 4-6 h | After B2a. |
| **B18** Dispatch tagging: the bind-before-capture contract | **What exists:** the collector accepts `dispatch` bindings in the capture-root inventory (`sessionId`, `workItemId`, `projectKey`, `attemptId`, `evidenceRef`, `validFrom/validUntil`; `capture-root-inventory.ts:18-26`), stamps `workItemId`, `dispatchProjectKey`, `workEvidenceRef` and the attempt ids on every event of that session inside the window (`:144-153`), allowlists them outbound (`analytical-metadata.ts:329-336`), and the cloud adapter reads them into `workItemId`/`attributionSource = dispatch` (`event-adapter.ts:32-34,49-50`). **The exact remaining gap:** nothing writes a binding (0% of usage rows carry `workItemId`, `PROD-FACTS-2.md:10`), there is no collector command or endpoint to upsert one (no `dispatch` reference in `cli.ts`), and a binding must exist **before** the session's events are tailed because enrichment happens at capture and the cloud repair lane fills only `project_key` (`cloud src/lib/attribution.ts:18-36`). **Deliverable:** (1) collector: `plimsoll dispatch bind` and `dispatch close` that upsert a binding with a bounded validity window into the inventory; (2) fleet tooling: the fleet-delegate/JEV launch hook calls `bind` with the bead id as `workItemId`, the repo remote hash as `projectKey`, the lane id as `attemptId`, and the runtime's session id captured at session start (Claude hooks / Codex rollout id), and `close` at lane end; (3) a proof on one tagged lane. | both (fleet tooling + collector) | `PROD-FACTS-2.md:10-12`; `VALUE-MAP.md:64`; `capture-root-inventory.ts:18-26,144-153`; `analytical-metadata.ts:329-336` | none | On one tagged lane, 100% of its usage records carry `work_item_id` and `project_key` end to end; G2's `workLinkedShare` rises on the data tenant; a binding created after capture is shown to be too late (red case). | 6-10 h (uncalibrated) | **Yes** for the collector half (a new command over an existing schema). **Fleet half: owner recorded; acceptance receipt NONE as of 2026-09-25; gated until an accepted native owner receipt exists (round-5 should-fix 2).** The launch path is `fleet-delegate` (v2, 2026-09-24), whose directory entry says "Studio0 lead sessions delegate" and whose canonical docs live on Studio0 (`/Users/utlyze/Library/Memory/ops/fleet/fleet-delegate/README.md`; `/Users/Shared/agent-tools/TOOLS-DIRECTORY.md`, row "fleet-delegate", read this round); its accountable native owner is therefore the **Studio0 lead session that maintains fleet-delegate**, the same identity this program reports to. No acceptance receipt exists as of 2026-09-25: this lane cannot message that session. The fleet half is **not startable** until the lead records, in Beads on B18 and by an Inbox `assignments.assign` to that owner, acceptance of the exact interface the collector half publishes: `plimsoll dispatch bind --session-id S --work-item-id BEAD --project-key REPO_HASH --attempt-id LANE --valid-from T [--valid-until T]` and `plimsoll dispatch close --attempt-id LANE`, called from the fleet-delegate launch hook before the worker's first event and at lane end, with the binding's `validFrom` earlier than the session's first tailed event (`capture-root-inventory.ts:144-153`, re-read this round: bindings apply only inside their window). |
| **B19** Allowance-window feed, spec | The attested allowance snapshot feed (source, account, window, used, limit, reset, attested at) and its page state; no implementation. | both | DECISION-METRICS.md §2 named future input | none | Spec reviewed by the lead; the "not connected" state rendered by B16. | 2-3 h | **Yes**; spec only. |
| **B20** Studio0 rehearsal and proof | Read-only unread-files query and offset census on Studio0; seven days of S0 samples on Studio0 (observe-only); copy Studio0's ledger to a host with proved fit; catch-up, conversion, certify against the oracle, deletion drill (first and second chunk), rebuild, abort and ladder drills, 24 h replay and the harness; produce `proof/studio0-copy/`. | both | PROOF.md §2-8; MIGRATION.md §3-5 | B3, B9b, B10b, B12, B13, B21, B22 | PROOF.md passes; peak growth, peak WAL, gross growth per day, the runway, the acknowledged/dead/pending split, receipts, roots and tombstone share and the converted size reported; Studio0's S1b numbers and window plan written with measured times. | 12-16 h worker, 2-4 days elapsed | After the light hosts and Studio6. |
| **B21** Tailer completeness: acceptance and integration | Accept `.163.81` (revisit queue, oversized-record skip) and `.163.82` (per-turn coverage-walk cap, per-source fairness) into **collector 0.7.42** (with `.163.84`; the round-5 handoff's release as cited by `input/review-r5/VERDICT.json` and the round-6 brief; `HANDOFF-R5.md` is not among this lane's inputs); run the catch-up per host in MIGRATION.md §5 order, measuring parse throughput and rows per MB on Studio5; declare every `unresolved:*` and never-read file as an epoch-wide unknown gap; wire the fields into claim v2 and G1 (with B1 and B6). Follow-up spec: a `last_parsed_observed_at` column per scan state and a per-source append-order proof, which would let a later round narrow the unresolved-file interval (ARCHITECTURE.md §3.6). | external (`.163.81`, `.163.82` owners) + collector | `LANE-ROOT-CAUSE.md:3-6`; `UNCOVERED-EVIDENCE.txt:16-53`; ARCHITECTURE.md §3.6, §6.4; `capture-frontier.ts:59-65,499-522` | the two lanes' acceptance | PROOF.md §8 items 1-5 on Studio5 live; per-host catch-up receipts; `bytesDeferred = 0` and a complete walk before each host's S1b. | 5-7 h integration; catch-up elapsed per host (unmeasured until Studio5) | **Coordination now**: read the lanes' results; no code in this program until they are accepted. |
| **B22** Durable unknownness: gap-and-cursor transaction, fault marker, restart rule | The tailers' commit path writes the gap row (or admits the row) and advances the cursor in one SQLite transaction; a pushed request whose gap cannot be recorded is answered 503; the in-memory fault, the `capture-fault.json` marker, the `capture_faults` table, the clean-shutdown marker unlink-and-recreate rule, `restart_unverified` and `gap_record_unavailable` on the claim, the `fault_interval` gap item, the zero-item summary batch that carries a claim alone (ARCHITECTURE.md §6.3); the **open-ended** interval for unresolved and never-read files: `interval_basis = 'epoch_open'`, `started_at_ms` = the epoch start, `ended_at_ms = null` until the file is parsed to its end (ARCHITECTURE.md §3.6; `out/fixtures/sf5_unresolved_file_open_gap.py` and `out/fixtures/b22_false_complete.py` run at the integration gate). | collector (cloud fields in B6) | ARCHITECTURE.md §3.6, §6.1-6.4; `collector-cli/src/capture-frontier.ts:24-32,49-55,294-304,499-522`; `outbox.ts:567-595`; `server.ts:340-364` | B0, B21 (lands after the two tailer lanes are accepted, under the same integration owner for `capture-frontier.ts`, `jsonl-byte-tailer.ts` and the tailers) | PROOF.md §4 "Watermark v2" and "Unresolved file" rows and §8 item 6: forced gap-write failure consumes nothing; crash before the next claim withdraws the older complete claim after restart; clean exit with an unresolved fault behaves the same; the old event inside a newly written file and the event stamped after the file's last write both lie inside the open declared gap, and no period after the last write is complete while the file is unparsed (`out/checks/counterexamples.log`, cases 13-14; `out/fixtures/b22_false_complete.py`). | 8-10 h | After B21's acceptance; the design and tests can be written now against a fixture. |

Totals: roughly **195-270 worker-hours, uncalibrated** (should-fix 4: B2a, B7, B10b and B13 are the least certain and are re-estimated after the Studio5 copy work and the membership-exchange drill; add a 30% schedule contingency until then; round 3 said 175-245; the seal lifecycle, tombstones and residue proof, actor parts and segment assignment, the fault and restart path, the runway and ladder, and the catalog's ceiling entries added the rest), plus elapsed time that no worker can compress: seven days of growth samples per host before its S1b (overlapping across hosts; B1 is already running on the light hosts), the tailer catch-up per host (unmeasured until Studio5), a 24 h S2 per host, a 7-day observation window per host and per tenant (S6, overlapping across hosts), the 7-day F window per host, W_retire = 14 days before retiring old tables and deleting cloud rows, and the rehearsal's 2-4 days. Calendar estimate to Studio0: 9-12 weeks after B0 is signed off, **uncalibrated** until the copy and membership-exchange drills have run; with the 30% contingency the honest range is 9-16 weeks. Every effort cell is an estimate, not a commitment.

## 2. Critical path and bottleneck

Drawn from the dependency column and checked by `out/checks/counterexamples.py` case 21 (every arrow below is an edge of the table; the table is acyclic; the topological order is in `out/checks/counterexamples.log`).

```
B0 ──► B2a ──► B2b ──► B3 ──► B5 ──► B9b ──► S6 (7 d) ──► B11 ──► S9 ──► B20 ──► Studio0
 │      ▲       ▲       ▲      ▲                             ▲
 │   B10a       │       │     B9a ───────────────────────────┤
 │          B13 ──► B2b │                                    │
 │          B21 ──► B3 ─┘   (external .163.81/.163.82; catch-up; then B21 ──► B22 ──► B11)
 │                                                            │
 └──► B6 ──► B7 ──► B8 ──► B9b ─────────────────────────────┤
              │                                              │
              └──► B10b (with B2b, B3, B10a) ────────────────┘
B1 ──► B2b (7 days of samples) ; B1 ──► B12 ──► B20 ; B15 ──► B16 ; B17 after B2a ; B18, B19 independent ; B14 deferred
```

- **Critical path**: B0 → B2a → B2b → B3 → B5 → B9b → S6 → B11 → S9 → B20 → Studio0, with B6 → B7 → B8 → B9b as the parallel branch that must also finish before S6, B13 gating B2b (its drills need B2a on a copy), and B21 as an external gate on B2b and B3 with B22 after it gating B11. About 100-130 worker-hours on the path plus the seven-day sample window, the catch-up, the observation window, F, the rehearsal and W_retire.
- **Bottleneck today**: B21, because no host can pass S1b until its unread files are read and the two tailer lanes are accepted, and the catch-up time is unmeasured; the seven-day growth samples cannot close until the catch-up rows are inside them. It is outside this program's code; the lead's lever is to have the `.163.81` acceptance run on Studio5 measure the throughput first. **Next bottleneck**: B0 (short, starts now) and then B2a, which now carries the seal lifecycle, the edges, the retirement proof and the converter, and B5, because `dashboard-projection.ts` is also the `.163.84` convergence surface. The critical path's hours are uncalibrated until the Studio5 copy drills (B2a, B13) and the membership-exchange drill (B7) report measured times; the path is re-drawn then. Recommendation: land the convergence change first and start B5 from its head; B2a-B4 give it runway.
- **Second constraint**: B8, because six cloud readers, the relation, segment assignment and the rights paths move at once. Split by reader if a second worker is available.

## 3. What can start now, in parallel with the economics lanes and the 0.7.42 collector work, and why each is safe

**Start order (the reviewer's round-5 first wave, `input/review-r5/VERDICT.json` `firstWave`, carried as written):** (1) B0 corrections and failing contract tests only; do not sign off the protocol, actor, timestamp, deletion or runway contracts. (2) B1 observe-only telemetry (running as eco-6hoxj.164.3); correct its runway derivation to the per-host G of BUDGETS.md §4.2 before any S1b hold. (3) B9a oracle and shape census, B10a hold code and copy test, B13 rebuild tool and copy drills; no live hold or S2. (4) B19 spec, B21 coordination for `.163.81`/`.163.82` in 0.7.42, B22 open-gap design and fixture only, B15 catalog draft, B18 collector command only until the fleet owner's acceptance. **Freezable now** (`freezable`): the pinned existing `<UR>` predicate and the current finance/reconciliation identity fields as test inputs, with the lane-2 re-pin when it exports the predicate; the narrow epoch-scoped day keys and the autoincrement target-reference shape, which the round-5 fixtures support. **Not freezable yet:** the day acknowledgement and deletion protocol and the actor, timestamp and runway contracts, until the round-6 fixtures are ported and green in both repositories and the lead signs off. B18's fleet half waits for an accepted native owner receipt.

| Bead | Why it is safe now | Shared surfaces to declare |
|---|---|---|
| B0 contract repair | Documents and failing tests only; the fixture test asserts against the inline predicate that already exists and re-pins when lane 2 exports its module; nothing it writes runs in production; nothing is frozen until the lead signs off. | none |
| B1 telemetry (running) | New module; one `/status` field; observe-only; reads tailer result fields and pragmas that already exist. It cannot change capture, uploads or deletion. The runway derivation reads its own samples. | `server.ts` status cache: one addition |
| B10a hold (code and copy test only) | One column, one early return, one predicate and a test on a ledger copy; **nothing is deployed and no host configuration changes** until that host's S1b runway holds. | `buffer.ts` prune: the early return (after B0 names the column) |
| B9a oracle, shape census and harness skeleton | Reads existing tables only; the oracle is an independent SQL that proves itself against today's snapshot; the census is the read-only query already run on Studio3 (`out/checks/ledger-shape-census.log`); nothing written to the ledger. | none until wired |
| B13 rebuild tool | Standalone script run against copies; the lease protocol is a design until B2a; nothing runs on a live ledger until the drills pass. | none until the leases are wired |
| B15 spec, token rules, volume state and catalog schema | Contract fields, the three catalog entry kinds, the normalisation rules and the unknown-volume gate decided (the reviewer's condition for freezing it); code waits for lane 3; the price entries are seeded from a file that already exists in the collector. | `contracts.ts` after lane 3 returns |
| B18 dispatch tagging, collector half | Adds a command over an existing schema and touches no capture path; a wrong binding only mis-attributes one lane, never a total. The fleet half waits for the recorded owner's acceptance of the published interface (B18 row). | Collector inventory (`capture-root-inventory.ts`) write path; the fleet-delegate launch hook (Studio0 lead, fleet-delegate maintainer) |
| B19 allowance spec | Document only. | none |
| B21 coordination | Reading the `.163.81`/`.163.82` results and running the read-only unread-files query on the two hosts not yet read (Studio0 with the lead, MacBook); no code in this program. | none (the query is read-only, `UNCOVERED-EVIDENCE.txt:2`) |
| B22 design and fixture tests | The transaction, marker and restart rules are specified against a fixture; the tailer commit-path change itself waits for B21's acceptance and its integration owner. | `capture-frontier.ts`, `jsonl-byte-tailer.ts`, the tailers (after B21) |

Not now: B2a (after B0 and B10a, days not weeks; fixture prototypes may begin), B2b and B3 (after S1b on the host: hold deployed under the runway, catch-up complete, B13 accepted), B6 (after B0), B4/B5/B8 (after the backfills), B10b/B11 (after receipts and observation), B12 (after B2a, B6 and B22), B16 (after B15 and B8), B20 (after everything). The reviewer's permission covers corrective beads and observe-only telemetry (`input/review-r3/REVIEW.md:85`); nothing in the start-now set freezes a contract, deploys a hold or starts a migration wave.

## 4. Ownership, interfaces and recovery

- **Integration owner**: the program lead on Studio0 merges in the order B0, B10a, B1, B9a, B13, B2a, B6, B22, B7, B2b, B3, B4, B5, B8, B17, B9b, B12, B10b, B11, B15, B16, B14, B20, running the full collector and cloud check lists after each merge (the economics lanes' lists in `LANES.md` plus the new tests). B21's lanes are merged by their own owners into collector 0.7.42 first.
- **Interface contracts**: the SQL in ARCHITECTURE.md §3 (collector tables), the item kinds, receipts, segments, parts, assignment rules and the coverage relation in §5 (upload), the claim v2 with tailer and fault fields in §6, the reader table and the rate catalog in §8, the budget block and the runway in BUDGETS.md §4-5, the deletion proof in §2.2, the hold and ladder in §2.4, and the flag names `lean.write`, `lean.read.finance`, `lean.read.dashboard`, `lean.facts`, `lean.upload_activity`, `summaries.*`, `usage_v2`. Any change is announced to the lead before merge.
- **Writers stay isolated**: collector beads own new files plus the named hooks; cloud beads own new paths plus the named additions; nobody edits `service.ts`, `event-adapter.ts`, `loader.ts` or `src/lib/usage-rollup/` (economics lanes); the session-sync record and its cloud upsert are not touched (ARCHITECTURE.md §10); `capture-frontier.ts` and the tailers belong to `.163.81`/`.163.82` until accepted, then to B22's integration owner.
- **Recovery path per bead**: the flag named in MIGRATION.md §2 and the regime column of §8; the hold holds every unacknowledged raw row until S9; old tables and files kept for W_retire; forward repair after S9 and after the tenant's first cloud deletion.
- **Stop conditions**: a certify or parity failure stops the stage on that host and is reported with the diff; a conversion-throttle pause is normal and logged; a budget breach in the advisory regime is reported, never acted on; the runway ladder acts as specified and is reported; a `partial` relation above 0.1% of sessions stops the S5 cloud switch; a failed identity audit stops deletion for that target; a conflict with a 0.7.42 or `.163.84` file is escalated to the lead rather than resolved locally.
