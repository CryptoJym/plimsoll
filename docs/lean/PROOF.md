# Acceptance proof (round 7 = B0 corrections on round 6)

**Bead:** eco-6hoxj.164.2 · **Round:** 6 · **As of:** 2026-09-25 MDT · This is the proof the program must produce before Studio0 is migrated and before the program bead closes. It is defined now so that every implementation bead knows what evidence it owes. Nothing here has been run.

**What changed in round 5.** The eight runnable fixtures in `out/fixtures/` (`run_fixtures.py`; results in `out/checks/fixtures-r4.log`, `fixtures-r5.log`, `fixtures-summary.json`) are the seed of B0's failing tests and are named in the rows they prove. The membership certify checks identity rows and the two covering edges per raw row (B1); the day-table rows are summed over epochs (B2); the retirement proof and the autoincrement key are drilled (B3); the rights drill covers undelivered members by binding stamp and the unallocated part (B4); the dashboard parity is stated per census class with the per-cutoff boundary rule (B5); G3 is asserted with the unknown-volume gate before any share (B6); the unresolved-file row uses the open gap (should-fix 5); a ladder-rollback parity drill is added (should-fix 1). Cloud pins are `067a8a4`; every citation re-validated this round.

**What changed in round 6.** The seven round-6 fixtures join the eight round-5 ones as B0's seed (§4 "Round-5 and round-6 fixtures" row). The ledger-growth expectation is 58.6% of raw bytes on a Studio1-shaped mix (§3); the tombstone count includes usage tombstones (§3); the abort bound is pre-S2 bytes plus the raw capture since (§3, §6 item 7); the deletion drill acknowledges the day target only through its `day_summary` receipt (§5 items 4 and 8); the dashboard parity names the canonical shape as the byte-parity set and folds non-ISO rows by their UTC day (§4); G3 withholds on a non-zero cache column under `no_cache_columns` (§4); the rights drill covers the offline rebind (§4); and §8 item 1 requires the open gap (`epoch_open`, null end) with `out/fixtures/b22_false_complete.py` at the B22 gate.

## 1. Two claims, two proofs

1. **Footprint**: on a copy of Studio0's ledger, the lean collector produces a ledger, RSS, CPU share and write rate inside the Studio0-scale post-migration targets (BUDGETS.md §2), measured before and after, with the conversion cost, the peak growth and WAL, the gross growth per day, the rebuild pause, the tombstone share and the never-delete share recorded.
2. **Parity**: every gate and metric in DECISION-METRICS.md is the same or better on the new path than on today's path, at the same frozen instant, on the same data, under the stated versioned semantics, and every raw row that is deleted has passed the deletion proof. "Better" means strictly more coverage with the reason recorded; a different count is a failure unless it is exactly the enumerated day-key correction.

## 2. Environment for the Studio0 rehearsal

| Item | Requirement | Evidence file |
|---|---|---|
| Copy | `sqlite3 <ledger> ".backup <copy>"` or `VACUUM INTO` at a quiet moment on Studio0, SHA-256 recorded; nothing else runs on Studio0 for this proof (`HOSTS.md:19`); a fresh `df` on Studio0 recorded (the old snapshots are already pruned; three remain); the read-only unread-files query (`UNCOVERED-EVIDENCE.txt:2-12`) and the shape census (`out/checks/ledger-shape-census.log`, with the class predicates of `out/fixtures/b5_lexical_boundary.py`) run on Studio0 and their results recorded. | `proof/studio0-copy.sha256`, `proof/studio0-df.txt`, `proof/studio0-uncovered.txt`, `proof/studio0-offset-census.txt`, timing |
| Host | A fleet Mac with measured free disk ≥ 2.5 × the copy size (184 GB) and fresh admission at start time (capacity check receipt). Not Studio0. | `proof/host-admission.json` |
| Collector | The lean collector build at a pinned commit, pointed at the copy, with capture tailers disabled (replay only), all flags at their production values. Old build at `92c33bf` (or the then-current release, `.163.84` if merged) available for the old-path queries. | `proof/build.txt` |
| Traffic replay | 24 h of captured raw rows from the copy's newest day replayed through the admission path at real cadence (or 4× speed with the budget scaled), to measure steady-state writes and RSS, not just conversion. | `proof/replay-manifest.json` |
| Frozen instant | Every parity query uses one instant T; for the local dashboard rows T is a UTC midnight so the schema-2 millisecond cutoff and the schema-3 day cutoff coincide (ARCHITECTURE.md §3.5). | `proof/frozen-instant.txt` |
| Source pins | Collector `92c33bf` (tree `67fd5a8`); cloud `067a8a4` (`4954995` + PR #151; `out/checks/source-pins.log`). | `proof/pins.txt` |

## 3. Footprint before and after

All measured on the copy, on the rehearsal host, with the same page size. Pass rules are post-migration targets; nothing here is enforced during conversion.

| Measure | Before (old build, copy as received) | After S1b catch-up (if Studio0 has unread files) | Peak during S2-S3 | After rebuild (S10) | After 24 h replay | Pass rule |
|---|---|---|---|---|---|---|
| Ledger bytes (DB + WAL + SHM) | measured | measured | **measured** (growth recorded; expected about 58.6% of raw bytes on a Studio1-shaped mix, less where activity rows dominate, `out/checks/plan-arithmetic.log` "Round 6"; `out/fixtures/s1b_runway_geometry.py`) | measured | measured | After rebuild and replay: within the Studio0-scale target; if above 12 GB the target is recalibrated before rollout (BUDGETS.md §3), never by deleting evidence. **The peak and the gross growth per day become Studio0's S1b numbers.** |
| Gross ledger growth per day, and the runway it implies | measured (7 days of live S0 samples on Studio0 itself, observe-only) | during catch-up | during conversion | n/a | during replay | recorded; `runway_days` computed with Studio0's fresh `df`; the hold may start only when the start rule holds (BUDGETS.md §4.2) |
| Peak WAL bytes during conversion and rebuild | n/a | n/a | **measured** | measured | n/a | recorded; feeds the reserve (2 × WAL term) and the rebuild precondition |
| `page_count`, `freelist_count` | pragma | pragma | pragma | pragma | pragma | freelist < 5% after rebuild |
| Bytes by table and index (`dbstat`, allowed on the copy) | measured | measured | measured | measured | measured | `dashboard_event_facts` absent; raw activity rows only inside R; density ≤ 3 MiB per 1,000 usage facts excluding receipts and roots but including tombstones, with receipts, roots and tombstones reported on their own lines |
| Row counts by table | measured | measured | measured | measured | measured | usage facts == usage rows admitted; every raw row has a fact or a live membership row (§4 row 1); tombstones == deleted raw rows, usage **and** activity (every deleted row leaves an identity tombstone, ARCHITECTURE.md §2.2), each with a `raw_retention_receipts` row |
| Acknowledged / dead / pending raw rows by month | measured | n/a | n/a | n/a | n/a | recorded; never-delete rows are carried, not deleted, and their bytes are reported as "not reclaimable by this program" |
| Receipts, root observations and tombstone bytes | measured | measured | measured | measured | measured | recorded; the B14 lever is sized from this |
| Activity-to-usage row ratio | measured | measured | n/a | n/a | measured | recorded; sizes tombstones (BUDGETS.md §3) |
| Abort recovery (§6 item 7) | n/a | n/a | measured | n/a | n/a | after `lean.write=off`, drop and rebuild, the file is at most its pre-S2 live bytes plus the raw capture since S2 (B1's admitted-row count × measured bytes per raw row), the new tables removed (`out/fixtures/sf_abort_rebuild_bound.py`) |
| RSS p50 / p95 (both processes, reported separately and summed) | 1,354 MB live baseline (`HOSTS.md:6`), copy baseline measured | during catch-up | during conversion | n/a | during replay | ≤ the Studio0-scale target p95 during replay; conversion peak recorded; which process drives RSS recorded |
| CPU core-equivalents, 5-min p95 | measured on old build during replay | during catch-up | during conversion | n/a | during replay | ≤ target during replay; conversion peak recorded |
| Write rate MiB/min, 15-min p95 | old build during replay | during catch-up | during conversion | n/a | during replay | ≤ target during replay |
| Conversion wall time, rebuild pause time | n/a | n/a | measured | measured | n/a | recorded; feeds the Studio0 window plan (MIGRATION.md §5); the incremental-copy alternative is chosen if the pause exceeds 15 minutes |

Light-host evidence uses the same table on Studio5 first (MIGRATION.md §5), with the light-class targets, and Studio5's run also records the parse throughput of the tailer catch-up (§8) and the first real runway.

## 4. Parity, metric by metric (frozen instant T)

Both paths are queried with `observed_at < T` and the same period. Old path = today's tables and readers on the old build; new path = the lean tables and readers. Tolerance is exact equality on integers and nanos; floating shares are compared at 4 decimals. Rows marked **exhaustive** are run over every row or session, chunked, never sampled.

| Item | Old-path query | New-path query | Equal or better rule |
|---|---|---|---|
| **Membership certify** (exhaustive) | every `buffered_events` row `(id, privacy_generation)` | live `summary_members (event_id, raw_generation)` with its `row_class`, its `summary_member_edges` rows, and for usage rows `usage_facts_local` | Both directions empty (`EXCEPT`-style by rowid range, matched by event id); every raw row has one live identity row and **exactly the two edges** its row class requires (base target + day target; `out/fixtures/b1_membership_edges.py`); no identity row, edge or fact without a raw row inside the retained window; every live `raw_rowid` pointer resolves to the row with that id; every edge's target exists; every tombstone's raw row is absent and its receipt present |
| **Recompute and identity certify** (exhaustive) | every session's raw rows, every (epoch, source, day)'s raw rows, every rollup bucket's raw rows | the stored open segments, `session_day_facts`, `model_day_facts`, `activity_day_facts` (keyed by epoch, summed over epochs and segments, `out/fixtures/b2_epoch_day_keys.py`), `activity_rollup_hourly`; every sealed segment's `member_sum_digest`, counts and residue | Every counter and sum of an open segment equal; for every sealed segment `Σ H(live) + Σ H(tombstones) = member_sum_digest` over its edges, counts add up, and the residue equals the digest minus the tombstones; a target whose last chunk was audited has no edges left and its tombstone identity rows remain; no `summary_segments` row has zero references unless its base row is inside its retention (`out/fixtures/b3_durable_target_refs.py`); part digests sum to the segment digest on the cloud |
| G1 coverage counts, faults and tailer completeness | Cloud: `usage_facts` states and `usage_rollup_hourly` counts (lane 2); collector: `bytesDeferred`, walk state (`rollout-tailer.ts:1263-1289`, `capture-frontier.ts:499-522`) | Same, plus claim v2 fields, declared gaps with `interval_basis`, and faults | Identical states; the new path adds declared gaps with `count_basis` and `interval_basis`, the fault state and the tailer fields (better); G1 is never `complete` while any host reports `bytesDeferred > 0`, an incomplete walk, an open fault or an unresolved file (whose gap spans the epoch) (§8) |
| G2 attribution shares (records, spend, normalised tokens per model) | Cloud rollup `work_linked`, `work_identity_missing`; `project_key` counts | Same plus the daily dimensional rollup with normalised tokens (B15) | Identical record and spend shares; token shares equal a hand computation under each `token_rule` on a fixture with OpenAI (cached ⊂ input) and Anthropic (input excludes cache) records |
| G3 pricing shares, bounded and unbounded remainders, **unknown-volume counts** | Cloud rollup `priced`, `unpriced`, `rate_unbound`, `unknown_n`; collector catalog fingerprint (`maintenance.ts:276-288`) | Same plus B15, the three `token_volume_state` counts and the rate catalog with price, ceiling and alias entries | Identical record shares; **the volume gate first** (`out/fixtures/b6_unknown_volume_gates.py`): a period with an admitted unpriced record whose token columns are null, or an `unknown`-rule **or `no_cache_columns`** record with a non-zero cache column (`out/fixtures/sf_no_cache_columns_guard.py`), or a `usage_live` placeholder, withholds the dollar comparison and every token share before any share is computed, while an all-known cohort at 95% passes; token shares on normalised tokens: the reviewer's fixture (85 priced input tokens all cached + 15 unpriced) reports 85%, not 91.9% (`out/checks/counterexamples.log`, case 9); every `catalog_estimate` fact resolves its `rate_catalog_ref`; a record on a model with a prefix price but no attested ceiling yields `rate_bound_kind = 'none'`, the unbounded remainder > 0 and a withheld dollar comparison; a record with an attested ceiling for its instant yields `attested_ceiling` and a bound equal to normalised tokens × the ceiling; a ceiling whose interval ended yields `none` (case 16) |
| M1 known spend by basis, current and previous period | Lane-3 projection reader | Same | Identical (usage envelope unchanged) |
| M2 spend by project | Lane-3 per-project summaries | Same | Identical |
| M2 spend by model / account / source | `usage_v1` groups restricted to `<UR>` rows; local by-model and by-source snapshots | B15 daily rollup; local `model_day_facts` and `activity_day_facts` | Identical sums and record counts per group |
| M3 budget burn inputs (daily sums, complete days; 7-13 day rule) | Daily sums from the hourly rollup; watermark per day | Same | Identical; the range rule tested at 7, 13 and 14 complete days; a day with an open fault is not complete |
| M4 cost per accepted outcome, rework | Not computable today (0% work items, `PROD-FACTS-2.md:10`) | New path over facts with `work_item_id` and the acceptance journal | Better: on a fixture with tagged dispatches, supersede chains and reopens, the value equals a hand-computed sum; distinct-item counting asserted |
| M5 spend without accepted result | Session sync totals + acceptance journal (cloud); no disposition today | Session summaries (segments summed) joined to the acceptance journal at read | Better: dispositions present with rule ids; for sessions with `session_stop`, ended-session sets and cost sums equal the old sync totals; `partial` sessions listed apart, never summed; a session with post-seal mutations flagged |
| M6 failures, retries, loops, spend after failure **by tool class and model** | `tool_attempt_facts` counts per session (local; capped at 100,000 attempts) | Summary counters per session, tool class and (class, model); `spend_after_failure_by_class_json`, `spend_after_failure_by_class_model_json` | Identical for sessions inside the cap; better beyond it (Studio0: 292,934 refused, `HOSTS.md:19`), each extra session listed; per-class and per-(class, model) spend equal a hand computation on a fixture with two overlapping classes and two models, the class total equals the sum over its models, and the session total is not the sum of the classes in the overlapping case (`out/checks/counterexamples.log`, case 8) |
| M7 cache shares with per-source rules | Token columns on usage facts | Same plus `token_rule` and `rate_catalog_ref` | Identical raw columns; shares equal a hand computation under each rule |
| M8 context percentiles, slope, above-threshold cost **by model** | Raw per-turn sums from usage rows grouped by native `turnIndex` (Codex), for **every** Codex session in the window | Turn summaries with `model_key`; `context_thresholds_by_model_json` | Identical per turn where native order exists; `ordered` basis sessions compared on session totals only; above-threshold cost per model equals the raw computation; a mixed-model turn is counted once under its dominant model |
| M9 daily totals versus baseline, `$X`, `T_X` and the zero baseline | Daily sums from the rollup | Same | Identical; on a fixture: a $30 / 35% day alerts, a $30 / 10% day and a $10 / 60% day do not; a weekday whose four-week median is $0 alerts on a $30 day by the absolute rule with the "no baseline" sentence, and a $0 day on a $0 baseline shows no deviation |
| Local dashboard, five snapshots, **schema 2 versus schema 3** (30, 90, 182, 365, 1,825 days) | `readSnapshot` on old build at T = UTC midnight (`dashboard-projection.ts:3635-3669`) | `readSnapshot` on new build (`DASHBOARD_SCHEMA_VERSION` 3) at the same T | (a) On Studio5, Studio3, Studio1, Studio4 (history inside raw retention): for every canonical-shape row (the SQL census's `canonical` class, ARCHITECTURE.md §3.5), byte-identical JSON on every retained field, including `sessions`, `repos`, `accounts` and their machine sets; for every other row (the census classes `day_move`, `before_own_midnight`, `non_iso`; zero on Studio3, `out/checks/ledger-shape-census.log`; counted per host at S0) the harness evaluates the boundary rule `(s >= C_str) != (Date.parse(s) >= C_ms)` per row and per cutoff in the oracle and asserts the schema-2/schema-3 difference equals exactly those flips and day moves (`out/fixtures/b5_lexical_boundary.py`) and nothing else; the only permitted additions are `schemaVersion`, `status.budget`, `status.gaps`, `status.faults`, `status.tailer`; any other difference fails. (b) On every host including Studio0: the schema-3 snapshot equals the **oracle** (an independent SQL over `buffered_events` and the compact segments with the day-aligned cutoff and the UTC day key, `proof/oracle.sql`), for every window; the oracle itself is proved equal to schema 2 at T on Studio5 and Studio1 first, on their `Z`-offset rows. (c) For windows longer than raw retention: schema 3 versus the oracle only, recorded as "more coverage beyond raw retention" with the reason; `totals.events`, `bySource.events` and `actionMix` for the full window equal the compact-segment history folded into `activity_day_facts` with its buckets re-keyed |
| Local dashboard at a non-midnight T | `readSnapshot` on old build | `readSnapshot` on new build | Documented difference confined to the straddling day: every group differs only by events observed between the millisecond cutoff and the day boundary (asserted by computing both cutoffs in the oracle), plus the census rows |
| Boundary timestamps at the cutoffs (fixture) | schema-2 string compare and slice (`dashboard-projection.ts:549-551,2185-2196`) | schema-3 UTC day and instant compare | `out/fixtures/b5_lexical_boundary.py` is the fixture: the reviewer's `2026-09-26T00:00:00+00:00` (excluded by the string rule, included by the instant rule, no day move), `2026-09-26T00:00:00-02:00`, `2026-09-26T00:00:00.0009Z`, `2026-09-26 12:00:00Z`, `2026-09-26T00:00+00:00`, `2026-09-26T24:00:00Z`, `2026-09-25T23:30:00-02:00`, `2026-09-26T01:00:00+02:00`, `Sat, 26 Sep 2026 00:00:00 GMT+00:00` and `1 Sep 2026 00:00:00 +00:00` (which the string rule excludes from every window), each at every midnight cutoff around its day; the harness runs the same script under the collector's bundled runtime and B9a records any `Date.parse` difference from `out/checks/node-date-parse.log` (round 5, node v20.20.2) and `out/checks/node-date-parse-r6.log` (round 6, node v26.8.1) |
| **Non-ISO timestamps in the day facts** (round 6) | today's `day()` slice (a non-date bucket) and string-rule window membership (`dashboard-projection.ts:549-551,2185-2196`) | the UTC day of the parsed instant (`dashboard-projection.ts:500-504`) | `out/fixtures/b5_non_iso_day_facts.py`: every schema-valid string, RFC-2822-style and month-name forms included, is folded into its UTC day and the day facts equal the raw-row oracle; only a string `Date.parse` rejects is a `contract_violation` gap, counted and listed by id in `conversion_rejects`; B9a re-runs the node measurement under the collector's bundled runtime |
| Local finance publication | Old projection on `dashboard_event_facts` | New reader on `usage_facts_local` | Identical rows, reason codes and totals; one red/green case per reason code (`PROVENANCE_LINEAGE_UNAVAILABLE`, `OBSERVED_INTERVAL_UNQUALIFIED`, `workspace_binding_mismatch`, `source_record_limit`) |
| Cloud admin recent strip | Latest 12 events (`loaders.ts:283-288`) | Latest 12 session revisions | Every session represented in the old strip appears in the new window; documented, versioned semantic change |
| Cloud manager view and experiment snapshot | Latest `EVENT_TAKE` / 1,000 events' action classes (`loaders.ts:311-320`; `mutations.ts:26-40`) | Session summaries' class counts over the declared period | Counts by class equal on a fixture where the sample lies inside the period; `metric_version = 2` recorded on new snapshots |
| `usage_v2` projection | `usage_v1` groups (`migration.sql:139-175`) | `usage_v2` groups | Usage-record groups equal `usage_v1` groups restricted to `<UR>`; activity counts equal `usage_v1` event counts for non-`<UR>` rows; `usage_v1` invalidated only after certify |
| **Cloud origin relation and segment assignment** (exhaustive per session and bucket; assignment per segment) | Cloud raw member set per session | Collector-origin session-level `memberSumDigest` / `deliveredMemberSumDigest`, the stored relation, `segment_ref` and the lacking-id list | Every session and bucket has a stored relation over all its segments; every cloud activity row has at most one `segment_ref`; on a fixture: identical sets → `equal` and every row assigned; dead deliveries → `collector_superset` with the lacking ids listed, and a late replay of a lacking id is assigned to the sealed segment, not to a new one; locally expired or ladder-released rows → `cloud_superset` after the id exchange; a mixed case → `partial` with the reader showing neither and the delete refusing; a reason-only report is not accepted as a gate (`out/checks/counterexamples.log`, case 4) |
| **Subject-rights export by actor parts** | Raw events for a test actor (`subject-rights.ts:218-242`) | Usage events + facts + actor parts of summaries, rollups and gaps where `actor_id` is the actor | Every session and usage record in the old export is present; the removed activity fields are listed; no payload text in the new tables; on a fixture where one install is bound A→B→C and another A→B→A (`device-install-actor-binding.ts:165-198,270-318`), with members ingested under each binding: A's, B's and C's exports each hold exactly the parts of the members ingested under that actor, B's part is present in both, A's A→B→A export holds two parts, a binding change after ingest moves no part, and the part digests sum to the segment digest (`out/checks/counterexamples.log`, case 5); and on `out/fixtures/b4_actor_at_capture.py`: an undelivered member captured under A is in A's export by its binding stamp (not B's), a member captured after the collector learned B is B's, a pre-stamp member of a rebound install is unallocated and in nobody's export, a never-rebound install's pre-stamp member is its single actor's, no part has basis `summary_ingest`, and the unallocated count per install is reported; and on `out/fixtures/b4_offline_rebind.py`: for seven orderings of capture, rebind, contact and delivery (the reviewer's offline capture at 250 after the rebind at 200 included) the delivered row and its undelivered twin name the same actor, a dead delivery replayed after a later rebind keeps its actor, and the `changed_at` → `heard_at` window is reported per rebind |
| Tenant erasure | Rows gone from raw and existing tables | Rows gone from every new table too (summaries, segments, parts, day rows, rollups, gaps, faults, quarantine); residue counts zero (`subject-rights.ts:599-601`) | No row for the tenant in any table |
| Actor impact report | Counts over raw tables | Counts over raw tables and actor parts | Counts equal for raw tables; parts counted; the report still says dry-run (no actor delete is promised) |
| Watermark v2, faults and the restart rule | `complete` iff every batch item held | Same, plus persisted gaps, faults, tailer fields; a claim with a `conflict` receipt leaves the watermark unchanged; a zero-item summary batch carries a claim alone | Never `complete` over a persisted gap (test with an injected gap and a later claim without it); with a forced gap-write failure: nothing is consumed (the cursor did not move, the unread bytes are still reported), the next claim has `through = null`, `unattested = 'gap_record_unavailable'`; then **kill −9 before the next claim** with the marker file unwritable: after restart the first claim is `through = null`, `unattested = 'restart_unverified'` until every fault is resolved and a whole-root walk completes, and the cloud shows the older complete claim withdrawn (`claimState` of a null `through` is `pending`, `contract.ts:199-201`); a clean exit with an unresolved fault behaves the same; `through = null` also when the last walk is incomplete or older than 24 h (`out/checks/counterexamples.log`, case 13) |
| Unresolved file with an old event, and with a future-stamped event | the round-3 write-interval gap | the open gap (`epoch_open`) | Inject a rollout in a day folder older than the discovery window containing one event stamped a week before the file's `uncovered_since` and one stamped five minutes **after** the file's last write, and mark the file unresolved: the declared gap has no end, contains both instants, `captureCoverageForPeriod` returns incomplete for a period after the last write, and G1 reports the machine's whole epoch through the present as unknown loss until the file parses; after the parse both events are admitted (the clamp keeps the later stamp, `normalizer.ts:203-214`) and the gap resolves (`out/checks/counterexamples.log`, case 14; `out/fixtures/sf5_unresolved_file_open_gap.py`) |
| Tagged model key | n/a | `activity_rollup_hourly`, facts, turns, day tables, local and cloud | A second NULL-model row for the same key is rejected; a literal `(none)` model and a NULL model produce distinct keys and both rows are accepted; the empty key `m:` is rejected (`out/checks/counterexamples.log`, case 10); a Postgres `NULLS NOT DISTINCT` test for the cloud |
| Durable identity, epoch-scoped keys and durable target references | n/a | `usage_facts_local`, `summary_members`, `summary_member_edges`, `summary_segments`, `session_summaries`, the day tables | The reviewer's rowid-reuse sequence inserts the second fact successfully (`out/checks/counterexamples.log`, case 1); the same session id in two installation epochs is two session rows and two base keys (case 7); the same day, source, model and segment in two epochs is two day rows (`out/fixtures/b2_epoch_day_keys.py`); a retired segment's `target_ref` is never issued again and a referenced segment cannot be retired (`out/fixtures/b3_durable_target_refs.py`) |
| **Round-5, round-6 and round-7 fixtures** (B0's seed) | the previous round's rule | this round's rule | `fixtures/run_fixtures.py`: every round-5 script exits 1 under `--rule r4` and 0 under `--rule r5`, every round-6 script exits 1 under `--rule r5` and 0 under `--rule r6`, and the round-7 scripts (`b4_offline_rebind.py`, `s1b_runway_host_bound.py`) exit 1 under `--rule r6` and 0 under `--rule r7` (`checks/fixtures-r4.log` … `fixtures-r7.log`, `fixtures-summary.json`); B0 ported each as a **pending** test in the repository that owns the rule (`CONTRACTS.md` C6): the bead named in the test removes its `pending` marker when it turns green |
| Predicate parity | `<UR>` text in the cloud (`loader.ts:72-79`, or `USAGE_RECORD_PREDICATE_SQL` once lane 2 merges) | `<UR>` text in the collector | Byte-identical; one pinned hash in both repos; an amount-bearing `otel_span` classifies as a usage record on both; the lane-2 export, when present, hashes to the pinned value or B0 re-freezes |
| Upload replay after rollback | Idempotent by id | Same | Zero new duplicates on the cloud after a rollback-and-resend drill (`PROD-FACTS-2.md:16` baseline: 0 duplicates); after the tenant's first cloud deletion, a resend of activity rows from an S7 install is refused with `activity_lane_closed` and re-inserts nothing |

## 5. Deletion-proof drill (on the Studio5 copy first, then the Studio0 copy)

1. **Hold red/green (B10a).** With `retentionDays = 3650` and with `retention.hold` set, run the prune with raw rows that are (a) pending in the outbox, (b) dead, (c) unmarked, (d) acknowledged, all older than 90 days: none is deleted, `/status` shows the hold. Clear both and run today's predicate: row (a) is deleted at age (`collector-cli/src/buffer.ts:3008-3013`). Both outcomes recorded.
2. **Hold-release ladder.** With the hold set and a synthetic runway under 5 days, run the ladder's prune: only row (d) is deleted, with receipt reason `lean_hold_release_acked` and, where membership exists, a tombstone; rows (a)-(c) and every member of an open target stay (`out/checks/counterexamples.log`, case 12). Under 2 days the abort path runs (§6 item 8).
   2b. **Ladder rollback parity (should-fix 1).** After the rung-2 release on the copy, flip `lean.facts=on` and `lean.read.dashboard=off`: assert the rebuilt old-path snapshot equals the pre-release old-path snapshot minus exactly the rows receipted `lean_hold_release_acked`, that the sealed segments and the cloud still hold the full pre-release count, and that `/status` discloses the release set (`out/fixtures/sf1_ladder_rollback_parity.py`).
3. **Receipt-table rebuild (B10b).** On a copy whose `raw_retention_receipts` carries the original CHECK, apply the rebuild transaction; assert the old rows survive, the old and the three new reasons insert, and the projection's raw-delete trigger is recreated on reopen (`out/checks/counterexamples.log`, case 11).
4. Run the lean retention job on a copy where at least one target is open, one is dirty (a raw row updated after its fold), one delivery is dead, one segment receipt is `conflict`, one day target whose `day_summary` receipt was `conflict` (quarantined, unacknowledged; `out/fixtures/b1_day_target_receipt.py`), one usage row's envelope digest differs from its fact, one segment has a post-seal mutation, and one session's relation is `partial`. Assert: none of those rows is deleted; each is listed with its reason in `/status`.
5. Seal and acknowledge a segment, then insert a late raw row for the same session below `high_water`. Assert: the sealed segment is untouched, the row is folded into segment 2 of the same base key, and segment 1's members remain deletable by the proof.
6. **Sessionless fence.** Seal and acknowledge an `activity_rollup_hourly` bucket segment, then update one of its member rows. Assert: the mutation is recorded in `sealed_member_mutations` through the membership lookup by event id, `post_seal_mutations` is bumped, the bucket is listed, and nothing of it is deleted until the mutation record is acknowledged with the session or bucket row's next revision.
7. Corrupt one membership row's payload digest on a copy. Assert: the per-row check refuses that row, nothing in the chunk is deleted, the target is marked `audit_failed`.
8. **First chunk.** Delete a fully proved chunk of a target with more than 5,000 live members; include a row whose day target is still open and assert it is refused and named (`out/fixtures/b1_membership_edges.py`); assert that the day target's `acked_revision` was set by a `day_summary` receipt in an upload batch and by nothing else (`out/fixtures/b1_day_target_receipt.py`). Assert: the identity audit before the chunk passes; the five snapshots, the finance publication and a test actor's export are unchanged before and after; the chunk's rows are tombstones with receipts; `capture_root_observations` for them remain; every fact of the chunk has `raw_rowid = null`; the residue equals the digest minus the tombstones.
9. **Second chunk.** Delete the next chunk of the same target. Assert: it passes on the seal, the per-row checks and the residue, without recomputing anything from the surviving members (the round-3 design refused here); after the last chunk `Σ H(tombstones) = member_sum_digest`, the residue is zero, the target's tombstone edges are dropped and its identity rows remain; then assert the retirement proof refuses the target while its base row is inside retention and a day target while it has children, and that a later segment never receives the number (`out/checks/counterexamples.log`, case 2; `out/fixtures/b3_durable_target_refs.py`).
10. **Rowid reuse.** After step 9, insert new raw rows until SQLite reuses a deleted rowid. Assert: the new rows' facts and membership are written into open segments, the old tombstones are untouched, and a target whose `high_water` exceeds the reused rowid is handled as a late row (step 5).
11. **Re-admission.** Push (hook) an event whose id was deleted in step 8, first with the same payload, then with a different payload. Assert: the first is refused as a duplicate with no new raw row and `readmissions = 1` on the tombstone; the second is admitted quarantined, writes an `activity_readmission_conflicts` row, is withheld from upload and is never selected for deletion (`out/checks/counterexamples.log`, case 3).

## 6. Rebuild, rollback, abort and runway drills (on a copy, before any live rebuild and before any host starts S2)

1. Kill −9 during `VACUUM INTO`: the partial target is discarded, the source passes `integrity_check`, capture resumes.
2. Non-empty WAL at checkpoint time: the rebuild refuses until `wal_checkpoint(TRUNCATE)` reports an empty WAL.
3. Free-space refusal: with a quota below live bytes × 1.2 + WAL high-water the precondition refuses and `/status` reports the shortfall.
4. Reopen failure after the swap: the rename-back path restores the old file before any writer resumes; nothing lost.
5. Writer pause: hooks and OTLP pushed during the pause land in the spools; after resume they drain and the capture claim returns to a non-null `through` with no gap. Every one of the 31 writer modules (`out/checks/writer-modules.log`) is exercised during the pause and observed to hold and release its lease.
6. Post-resume old file: assert the runbook refuses to swap `.pre-lean-<date>` back once any writer has resumed (forward-repair boundary).
7. **Abort after a copy-ready S2/S3 (should-fix 3).** On a copy taken through B2a's writer and converter (no host has entered S2), set `lean.write=off`, drop the new tables, run the rebuild: the file returns to at most its pre-S2 live bytes plus the raw capture since S2 (raw rows admitted since S2, counted by B1, × the measured bytes per raw row; `out/fixtures/sf_abort_rebuild_bound.py`); the measured peak is recorded as the abort's headroom need, which the runway subtracts.
8. **Runway ladder.** On the same copy with a synthetic volume limit and a replayed growth rate, walk the runway from above 30 days to below 2: assert the converter pauses at 10, the acknowledged-only release runs at 5 with receipts and tombstones and nothing unacknowledged gone, and the abort runs at 2 returning the host to S0 with the reason in `/status` (`out/checks/counterexamples.log`, case 15 for the arithmetic). **Round 8:** assert that while the converter runs the published runway subtracts `G_owed` computed from the converter's own byte counter and the raw rows admitted since the census (`runway.g.owed` falls only as the converter writes, never as the live writer dual-writes), against a runway **oracle** computed on the copy from the true remaining conversion bytes independently of the collector's arithmetic (the drill replays S2 for two days before S3 so dual-write pages exist), so that no rung fires later than the oracle and the published figure never exceeds it; and that `runway.g.gate` equals the figure in the preflight receipt from the host's own post-catch-up census (`fixtures/s1b_runway_host_bound.py`, round 8; collector `tests/contracts/lean/runway.contract.ts`).

## 7. Budget enforcement drill (post-migration only, on the rehearsal host after S10)

1. Set the class target to 90% of the converted size; confirm the reduction steps run in order with `/status` disclosure and that the never-delete set, the tombstones and the day-grain history are untouched.
2. Set the target to the converted size; replay traffic; confirm activity admission stops, a **counted** `capture_gap` is written and uploaded, usage records keep flowing into the reserve band while it is physically present, and the cloud strip shows the gap. Then raise the target and confirm resumption with no undeclared loss (replayed rows − admitted rows == declared dropped rows).
3. Remove the band's physical backing (a synthetic volume limit below `1.05 × C + 2 × WAL_24h + 64 MiB`); confirm the collector declares the band exhausted, stops tailing, declares an **unknown-count** gap (`count_basis = 'unknown'`, `dropped_usage_rows = null`), and the cloud shows "unknown loss", never a number; `/status` shows `reserve.present = false`.
4. **Full volume.** Fill the volume so that the gap write itself fails: assert nothing was consumed (cursor unchanged, unread bytes still reported), the fault is set, the marker file write fails without crashing, `error_samples` and old `metric_samples` are freed, the gap write is retried, and every claim in between has `through = null`; then free space and assert the fault row, the gap and the claim recover.
5. Force RSS above the target (large synthetic session); confirm the worker checkpoints and restarts without losing an admitted row, and that the supervisor's RSS is reported on its own line.
6. Conversion throttle: on a pre-migration copy, confirm that a ledger above every cap converts without pausing on size, and that it pauses on CPU, write rate, the reserve and the runway with the reason in `/status`.

## 8. Tailer completeness drill (on Studio5 live first, then every host before its S1b)

1. Run the read-only unread-files query (`UNCOVERED-EVIDENCE.txt:2-12`) before and after the `.163.81`/`.163.82` catch-up; assert every non-`unresolved` class reaches 0 files and 0 MB, and every `unresolved:*` file has a `capture_gaps` row with the matching reason, `count_basis = 'unknown'`, `interval_basis = 'epoch_open'`, `started_at_ms` = the epoch start, **`ended_at_ms` null** (open until the file is parsed to its end, when `resolved_at_ms` is set), and a file-key digest (never a path); `fixtures/b22_false_complete.py` (document half, repository-relative: collector `tests/contracts/lean/b22-documents.contract.ts`) and `sf5_unresolved_file_open_gap.py` (behavioural half: collector `tests/contracts/lean/capture-gaps.contract.ts`, cloud `tests/contracts/lean/capture-coverage.contract.test.ts`; `CONTRACTS.md` C5) run green at the B22 integration gate: a period after the file's last write is never complete while the file is unparsed.
2. Assert the last automatic pass reports `bytesDeferred = 0` and `deferredGenerations = 0` per source (`rollout-tailer.ts:1263-1289` and the equivalent fields of the other tailers), and the last whole-root walk is complete, younger than 24 h and covers every configured root.
3. Inject an unread file (a copied rollout in a day folder older than the automatic discovery window) and assert: the walk reports it, the frontier does not pass its first unread byte, the claim carries it, and G1 shows the machine as "not fully read" until the revisit queue parses it.
4. Record the parse throughput (MB/min) and the rows added per MB on Studio5; these size the catch-up on every later host (MIGRATION.md §5).
5. Assert a seven-day G1 with zero declared gaps is still reported `not complete` while any host's `bytesDeferred > 0` (a fixture with a stalled tailer), while an unresolved file exists, or while a fault is open.
6. **Gap-and-cursor atomicity (B22).** With a forced SQLite failure on the gap insert, assert the tailer's cursor did not advance and the file's unread bytes are reported unchanged by the next walk.

## 9. Pass criteria and evidence

The program passes when every row in §3 meets its rule on the Studio0 copy and on Studio5 live, every row in §4 is identical or recorded as better with its reason under its stated contract version (the census correction enumerated where it applies), and §5, §6, §7 and §8 show no undeclared loss, no unproved deletion, no rebuild from a partial member set, and no unread input called captured. Evidence is a directory `proof/<host>/<date>/` with: raw measurement logs, the SQL or CLI used for every query on both paths (including `oracle.sql`), the frozen instant, both outputs, a diff, the drill transcripts, and `MANIFEST.sha256`. The lead's acceptance of that directory closes bead B20 and gates the Studio0 live window.
