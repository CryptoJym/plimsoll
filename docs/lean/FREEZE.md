# Freeze list after B0 round 8 (round 14, eco-6hoxj.164.4)

**As of:** 2026-09-26 MDT. Start point: the round-13 `FREEZE.md`, the lead's signatures (the eight round-9 "freezable now" items, and
**C4**), and the independent read of C1 after round 13 (`input/read-c1-r13/VERDICT.json`: **NOT_YET**; the deadlock retry and the
refusal's ledger confirmed to close both round-12 counterexamples in the reader's own PostgreSQL 17.11 SQL models, the r12 fixture 71/76
red and r13 76/76 green with every older printed check unchanged; C2, C3, C4, C5 and C8 byte-identical to round 12; two blocking items,
both about the two wire promises no test bound, and two should-fixes). A contract is **freezable** when its rule is stated once, its
fixture is green under the current rule, its pending test exists in the owning repository, and no open review finding touches it.
**Freezing is the lead's signature**, not B0's: this list says what is signed, what is ready for that signature and what still needs a
review round. Nothing is frozen by this document.

## Signed by the lead (recorded here; nothing in this round changes their text)

| Contract | Where |
|---|---|
| The pinned `<UR>` usage-record predicate and the finance/reconciliation identity fields as test inputs, with the lane-2 re-pin on merge (C8) | CONTRACTS.md C8; ARCHITECTURE.md §3.1, §10; `fixtures/usage_record_predicate.sql` in both repositories; cloud and collector `usage-record-pin` tests |
| Epoch-scoped day keys and the autoincrement `target_ref` | ARCHITECTURE.md §2.2, §3.3, §3.5; collector `schema.contract.ts`, `membership.contract.ts` |
| The `day_summary` item and its receipt shape, and the collector's acknowledgement-only-through-receipts rule | ARCHITECTURE.md §5.2; cloud `activity-summary.contract.test.ts`; collector `receipts-and-ladder.contract.ts` |
| The S1b runway formula (C2 rules 2-3) and the census obligation (rule 1), with `C_conv` as a measurement; **the numbers stay open** (below) | CONTRACTS.md C2; ARCHITECTURE.md §2.4; BUDGETS.md §4.2-4.3; MIGRATION.md S1b, S3; `fixtures/s1b_runway_host_bound.py` (r9 25/25); collector `runway.contract.ts` |
| `conversion_rejects` DDL and retention (C3) | CONTRACTS.md C3; ARCHITECTURE.md §2.2, §2.3, §3.5; collector `schema.contract.ts`, `converter.contract.ts`, `conversion-rejects.contract.ts` |
| The open-ended unresolved-file gap (`epoch_open`) in the bead, the drill and the architecture (C5) | BEADS.md B22; PROOF.md §8; ARCHITECTURE.md §3.6; collector `b22-documents.contract.ts` (guard), `capture-gaps.contract.ts`; cloud `capture-coverage.contract.test.ts` |
| Token volume state and the G3 gate, including the `no_cache_columns` guard | DECISION-METRICS.md §0; ARCHITECTURE.md §3.1; cloud `token-volume.contract.test.ts` |
| The abort rebuild bound | MIGRATION.md §4 step 8; PROOF.md §6 item 7; collector `receipts-and-ladder.contract.ts` |
| **The joined-install scope of the pair and the null stamp before the first response (C4)** | CONTRACTS.md C4; ARCHITECTURE.md §5.2; `fixtures/b4_offline_rebind.py` case L; collector `actor-stamp.contract.ts`; cloud `actor-binding-stamp.contract.test.ts`. The read of C1/C4 passed it. Round 10 added only what its should-fixes asked. **Round 11 adds only a cross-reference** (a pre-re-join row delivered through a ledger the join could not link is held by C1 until the ledger is linked) **and one additive `/status` field** (`parkedRows`); the collector's promise the lead signed, keep the pair on rows already in the outbox at a re-join, is unchanged, and the judgment of such rows was always C1's. If the lead reads that cross-reference as a change to C4, the alternative is to strike it from C4 and let C1 alone carry the hold; every test binds C1. **Rounds 12, 13 and 14 change nothing in C4** (the three reads confirmed its round-11 changes as additive and its text byte-identical since, `checks/signed-sections.json`); C4's parenthesis counting the collector test's cases (12) is now behind C1's list (17) and is left as it stands. |

## Ready after one more independent read of the round-14 text

| Contract | Where | State |
|---|---|---|
| **The actor-ownership predicate (C1) with the null-stamp exception** | CONTRACTS.md C1 (guarantee 5, "Serialization" and "Scope" round 11; "The link is the release" round 12; the retry of an aborted transaction in "Serialization" and the ledger on the 400 in "Outside the ledger" round 13; the wire shapes, the default cap, the exact 503 answer and the fixed request context round 14; the C4 sentence and `/status` field round 11 added; C6); ARCHITECTURE.md §5.2, §11; MIGRATION.md; `fixtures/b4_offline_rebind.py` (r14 81/81; red under r6, r7, r8, r9, r10, r11, r12 and r13); cloud `actor-binding-stamp.contract.test.ts` (16 cases), `actor-binding-stamp-postgres.contract.test.ts` (9 cases, with `fixtures/join-route-child.ts`), `ingest-route-wire.contract.test.ts` (3 cases), `schema-additions.contract.test.ts` (5 cases); collector `actor-stamp.contract.ts` (17 cases) | The read of the round-13 text confirmed the deadlock retry and the refusal's ledger (both round-12 counterexamples closed in the reader's own PostgreSQL 17.11 SQL models, the r12 fixture 71/76, r13 76/76) and said **not yet** for two items: C6 named the 400's first field `error` on the cloud and `reason` on the collector with no mapping, and no test carried the route's body into the collector's parser; the default cap was checked only through a one-attempt override and the exhausted 503 as an error object, while the route's generic handler would have flattened it to `ingest_unavailable`. Closed in this round with exact tests on both sides of the wire, fixture checks red under round 13 and pending tests: **one field name, `error`, on both sides; a pending route test on the exact 400 body; a pending collector test parsing that serialized body through its own upload path, parking under its ledger, released by the next response naming X and delivering once; a 400 naming no ledger parks nothing; an unknown ledger id is opaque; the default cap bound by three forced aborts and never a fourth; the exact 503 answer through `collectorIngestErrorResponse` with `Retry-After`; the collector acknowledging nothing, parking nothing and retrying at `Retry-After`; the request context fixed at authorization while everything else is re-read on a retry.** The two body literals are byte-identical across the repositories (`checks/wire-body-pin.log`); the collector's answers to both bodies were checked against today's code (`checks/collector-wire-path-probe.log`). The design the five reads accepted (the pair, the sticky first-sighting fact, lock then read, the ledger key with the join's proof, `ON CONFLICT DO NOTHING` with a re-read, the cap, the refusal across ledgers with the hold, the whole-ledger merge into a canonical destination with the join's lock and the ledger on the response, the deadlock retry with nothing acknowledged before commit, the refusal naming the ledger it judged, the null-stamp exception, lifecycle-independent resolution, the erasure-only guard, retention until erasure) is unchanged. **Not listed as ready outright**: the wire shapes, the cap's binding and the fixed-context rule are new text no reviewer has read; one independent read of C1 round 14 is the remaining condition for signature. |

## Not freezable yet (needs a review round or a measurement)

| Contract | Why not |
|---|---|
| **The S1b runway numbers** as a gate that may start a live hold | The row widths (`F`, `T`, `S`, `SD`, `DR`, `RB`, `SEG`, `I`) are still column-width estimates and only Studio4's cardinalities are measured (its segment proxy is not: the census was not re-run in rounds 8-14). The fixture shows the rule overstates the runway when the true widths exceed 1.25 × the estimates (and does not self-correct), so no hold may start on any host before S2 has measured the widths on the Studio5 copy and B1's post-catch-up census has run on that host's copy (review r6 `firstWave`: "no live hold or S2"). |
| The deletion proof and the cloud residue proof as a whole | Its blockers were stated as conditions on freezing the actor and runway contracts, which the deletion proof's step 1 depends on through the day receipt; freeze after C1 is signed. |
| Claim v2, the fault marker and restart rule, the paged id exchange | Specified, not implemented, no fixture beyond `sf5` and `b22`; review r6 called them future contracts (R6-15). A fixture-backed round is needed before freezing. |
| The per-row width constants and the 1.25 factor | Estimates until S2 (BUDGETS.md §6); the fixture's width-sensitivity check is the reason. |
| B18's fleet half | Gated on an accepted native owner receipt (unchanged since round 6). |

## What the lead signs next

1. C1 (the actor predicate as the pair fixed at its first sighting, the sighting locking first and reading everything under the lock,
   the fact keyed by the ledger, ownership never crossing a ledger with the hold and the whole-ledger link as its release, a deadlocked
   transaction retried from its start with nothing acknowledged before commit and the request context fixed at authorization, the
   refusal naming the ledger it judged with one field name on both sides of the wire, the exhausted retry's exact answer,
   lifecycle-independent resolution, the erasure-only guard; the null-stamp exception): ready **after one independent read of the
   round-14 text** (C1 "Serialization" and "Outside the ledger" round 14, the round-13 text the read confirmed, C6's surfaces).
2. Nothing else changes hands in this round: C4 and the eight round-9 items are signed; the runway numbers and the rest stay open as above.

## First-wave conditions from reviews r1 and r2 and the four reads of C1, and their state

- Review r2: "the first wave can continue"; B10a's hold tests append and fail only at `hold_reason`; B1 builds `G_owed` as in round 8 (the formula is unchanged since) and reads `C_conv` from the converter's measured checkpoint.
- Review r1: "fix the pending-test helper before B10a relies on its tests": done in round 8 and completed in round 9; round 10 added one green guard (join.ts activation with a fake cloud) for the new collector test's premise.
- The read of C1/C4: "neither [Postgres-proof item] blocks signing" but both must be fixed "before B6 removes the markers": done in round 10 (`checks/postgres-harness-repaired-probe.log`).
- The read of C1 after round 10: its two blocking items were closed in round 11 and confirmed by the read of C1 after round 11; its should-fixes are done: BEADS.md's B6 row says the fact is keyed by the ledger (it said "uploader"), `cluster()` stops its cluster on any set-up failure, the join route's token binding has integration cases through a child process (proved runnable against today's route, `checks/join-route-child-probe.log`), and "another Mac never enters the ledger" is stated with its condition, that the previous install's key stays secret, together with the first-sighting guarantee per ledger.
- The read of C1 after round 11: its one blocking item was closed in round 12 and confirmed by the read of C1 after round 12 (the link moves a ledger, never an install, into a canonical destination; the join's lock; the ledger on the response); its should-fix is done: the Postgres proof seeds P server-bound to B, as the S8 ordering has it.
- The read of C1 after round 12: its two blocking items were closed in round 13 and confirmed by the read of C1 after round 13 (a deadlocked C1 transaction retried from its start, at most three attempts, nothing acknowledged before commit, the exhausted retry a 503; the 400 naming the ledger it judged and the collector parking under that wire value); it had no should-fix.
- The read of C1 after round 13: its two blocking items are closed above (one field name on both sides of the 400 with the route and parse tests; the default cap and the exact 503 answer bound at the route and in the collector); its two should-fixes are done: the fixture's park no longer parks a 400 that names no ledger, an unknown ledger id is specified and tested as opaque, and the request context fixed at authorization is an explicit exception to "every judgment input re-read". Its notes stand: the pending state of every B6/B2a test is expected and is not an implementation proof; the SQL models of rounds 11-13 are carried unchanged.
